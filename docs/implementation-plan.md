# ISETCam MATLAB-to-Python Migration Plan

## Current Status
- Milestone one is complete in the repository: the curated Octave parity suite is green and the handoff docs/examples are in place.
- Current execution focus has moved into post-milestone expansion on broader scene support, sensor presets, metrics, and ray-trace optics.
- Phase 4 metrics coverage is now started with `src/pyisetcam/iso.py`, headless `ISOFindSlantedBar(...)` / `ieCXcorr(...)` / `edge_to_mtf(...)` for `s_metricsEdge2MTF.m`, and the shared `scene_create('slanted bar', ...)` path now matches upstream MATLAB's odd-sized, origin-centered construction.
- That same Phase 4 metrics wave now also includes headless `ISO12233(...)` / `ieISO12233(...)` for `s_metricsMTFSlantedBar.m`, with curated Octave parity on the direct RGB slanted-bar path, the `ieISO12233(ip, sensor, ...)` sensor-space path, and the monochrome direct path using case-scoped normalized-MAE / relative contracts on the ESF, LSF, MTF, MTF50, and aliasing summaries.
- That same metrics compatibility surface now also includes legacy `ISO12233v1(...)` / `ieISO12233v1(...)` wrapper names on top of the current headless slanted-edge implementation, so the older MATLAB file surface is covered without carrying a second ISO algorithm fork in Python.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_metricsMTFPixelSize.m`, covering the fixed-die-size monochrome slanted-bar sweep across 2/3/5/9 um pixels, the replayed per-pixel-size ROI geometry, Nyquist and MTF50 summaries, and the normalized luminance MTF profiles using case-scoped MTF tolerances.
- That same Phase 4 metrics wave now also includes headless `pixel_v_per_lux_sec(...)` / `pixel_snr_luxsec(...)` for `s_metricsSNRPixelSizeLuxsec.m`, with curated Octave parity on the monochrome 2/4/6/9/10 um pixel sweep, the returned lux-sec sensitivity curves, and the corresponding total/read/shot SNR summaries.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_metricsMTFSlantedBarInfrared.m`, plus `ie_read_color_filter(..., 'IRBlocking')` compatibility so the NikonD200IR multispectral slanted-bar setup, fixed-ROI direct ISO12233 branch, and IR-blocked auto-ROI `ieISO12233(...)` branch all run headlessly.
- That same Phase 4 metrics wave now also includes headless `camera_mtf(...)` / `camera_acutance(...)` together with `cpiq_csf(...)` / `iso_acutance(...)` for `s_metricsAcutance.m`, with curated Octave parity on the default camera slanted-edge workflow, the returned luminance-MTF/CPIQ profiles, the `sensorGet(..., 'h deg per distance', ...)` conversion, and the final acutance scalar.
- That same Phase 4 metrics wave now also includes headless `camera_color_accuracy(...)` / `cameraColorAccuracy(...)` plus `macbeth_compare_ideal(...)` / `macbethCompareIdeal(...)` for `s_metricsColorAccuracy.m`, with curated Octave parity on the default Macbeth-camera workflow, normalized white-point recovery, per-patch Delta E / LAB outputs, and the returned sRGB Macbeth comparison patches.
- That same Phase 4 metrics wave now also includes headless `macbeth_color_error(...)` / `macbethColorError(...)` for `s_metricsMacbethDeltaE.m`, with curated Octave parity on the default scene/OI/sensor/IP current-matrix workflow, recovered sensor CCM, normalized white-point recovery, per-patch Delta E / LAB outputs, and processed-image RGB summary statistics.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_metricsSPD.m`, covering the daylight `4000:500:7000 K` comparison sweep for both the D4000-reference and fixed-D65-white-point branches together with the returned angle, CIELAB Delta E, and mired curves.
- That same Phase 4 metrics helper surface now also includes headless `exposure_value(...)` / `exposureValue(...)`, `photometric_exposure(...)` / `photometricExposure(...)`, and `chart_patch_compare(...)` / `chartPatchCompare(...)`, matching the direct MATLAB exposure/chart mini-family used around the older metrics workflows.
- That same Phase 4 metrics helper surface now also includes the headless metrics-window compatibility layer: `metrics_camera(...)` / `metricsCamera(...)`, `metrics_compute(...)` / `metricsCompute(...)`, `metrics_get(...)` / `metricsGet(...)`, `metrics_set(...)` / `metricsSet(...)`, `metrics_description(...)` / `metricsDescription(...)`, `metrics_get_vci_pair(...)` / `metricsGetVciPair(...)`, `metrics_masked_error(...)` / `metricsMaskedError(...)`, `metrics_show_image(...)` / `metricsShowImage(...)`, `metrics_show_metric(...)` / `metricsShowMetric(...)`, `metrics_save_image(...)` / `metricsSaveImage(...)`, `metrics_save_data(...)` / `metricsSaveData(...)`, `metrics_close(...)` / `metricsClose(...)`, `metrics_refresh(...)` / `metricsRefresh(...)`, and `metrics_key_press(...)` / `metricsKeyPress(...)`, matching the older MATLAB metrics GUI helper surface with headless payload returns.
- That same Phase 4 metrics wave now also includes headless `camera_vsnr(...)` / `cameraVSNR(...)` for `s_metricsVSNR.m`, with curated Octave parity on the default camera uniform-field sweep, the stable ROI geometry, normalized VSNR / reciprocal-Delta-E curves across the three light levels, and processed-image RGB summary statistics.
- That same camera compatibility surface now also includes headless `camera_clear_data(...)` / `cameraClearData(...)`, `camera_compute_srgb(...)` / `cameraComputesrgb(...)`, and `camera_compute_sequence(...)` / `cameraComputeSequence(...)`, matching the older MATLAB wrapper layer for computed-state reset, ideal-versus-rendered sRGB replay, and multi-frame exposure sweeps on top of the current Python camera pipeline.
- That same Phase 4 metrics wave now also includes headless `scielab_rgb(...)` / `scielabRGB(...)` plus the underlying `scielab(...)` path for `t_metricsScielab.m`, together with MATLAB-style display `rgb2xyz` / `white point` accessors and curated Octave parity on the LCD-Apple `hats.jpg` versus `hatsC.jpg` S-CIELAB tutorial workflow.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_rgb2scielab.m`, covering the `crt.mat` S-CIELAB script path for `hats.jpg` versus `hatsC.jpg`, the returned mean Delta E, the script’s `Delta E > 2` summary statistics, scene geometry, display white point, and a canonicalized error-profile slice.
- That same Phase 4 metrics wave now also includes headless `dac_to_rgb(...)` / `dac2rgb(...)` and `image_linear_transform(...)` / `imageLinearTransform(...)` for `s_scielabExample.m`, with curated Octave parity on both the script's `scielabRGB(...)` branch and its explicit `dac2rgb -> displayGet('rgb2xyz') -> imageLinearTransform -> scielab(...)` walkthrough, including the returned scene geometry, white point, Delta E summaries, and canonicalized filter/error-profile slices.
- That same Phase 4 metrics wave now also includes headless `sc_prepare_filters(...)` / `scPrepareFilters(...)` for `s_scielabFilters.m`, with curated Octave parity on the 101-sample filter point-spreads, the 512-sample MTF branch, and the `distribution` / `original` / `hires` Gaussian-parameter variants through normalized center-row and peak summaries.
- That same Phase 4 metrics wave now also includes the legacy SCIELAB helper layer: `change_color_space(...)` / `changeColorSpace(...)`, `cmatrix(...)`, `gauss(...)`, `get_planes(...)` / `getPlanes(...)`, `ie_conv2_fft(...)` / `ieConv2FFT(...)`, `pad4conv(...)`, `pre_scielab(...)` / `preSCIELAB(...)`, `sc_resize(...)` / `scResize(...)`, `separable_conv(...)` / `separableConv(...)`, `separable_filters(...)` / `separableFilters(...)`, and `visual_angle(...)` / `visualAngle(...)`, matching the older MATLAB preprocessing/filter glue used around the SCIELAB scripts.
- That same Phase 4 metrics wave now also includes headless `scene_add(...)` / `sceneAdd(...)` plus `sceneGet(..., 'illuminant xyz')` for `s_scielabMTF.m`, with curated Octave parity on the harmonic `1:32 cpd` sweep, the `remove spatial mean` scene-add path, the illuminant XYZ/SPD setup, and the resulting CIELAB versus S-CIELAB Delta E curves.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_scielabPatches.m`, covering the 7x7 uniform-patch illuminant-perturbation sweep from `sceneCreate('uniform')`, the returned illuminant XYZ/SPD setup, the paired CIELAB / S-CIELAB Delta E surfaces, and the script’s quantized S-CIELAB summary vector.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_scielabMasking.m`, covering the fixed `4 cpd` / `0.8` harmonic-mask setup, the `sceneAdd(..., 'remove spatial mean')` target-combination path, the script's doubled white-point convention, and the resulting CIELAB / S-CIELAB target-contrast curves.
- That same Phase 4 metrics wave now also includes headless `colorTransformMatrix(...)` / `scOpponentFilter(...)` / `scComputeSCIELAB(...)` plus `srgb2xyz(...)` for `s_scielabTutorial.m`, with curated Octave parity on the multispectral Stuffed Animals scene through OI/sensor/IP, the Gray World rendered-RGB handoff, the 50 samples-per-degree SCIELAB filter setup, the opponent/filtered-image summaries, and the final SCIELAB LAB output.
- That same Phase 4 metrics wave now also includes curated Octave parity on `s_scielabHarmonicExperiments.m`, covering the sweep-frequency scene through diffuser-blurred OI, Gray World IP rendering, the script's three opponent-channel scaling branches, and the resulting padded S-CIELAB error-surface summaries.
- `scene_from_file` / `sceneFromFile` is implemented for RGB, multispectral, and monochrome emissive-display inputs.
- Additional MATLAB-style scene patterns are now implemented: `uniform bb`, `uniform monochromatic`, `line ee`, `line ep`, `bar`, `point array`, `grid lines`, `white noise`, `harmonic`, `frequency orientation` / `freq orient`, `sweep frequency`, `star pattern`, and `reflectance chart`.
- Phase 3 scene coverage now also includes MATLAB-style `spd_to_cct` / `spd2cct` for the blackbody-CCT workflow in `s_sceneCCT.m`, with curated Octave parity on the 3500/6500/8500 K single-spectrum estimates and the 4500:1000:8500 K multi-spectrum sweep.
- That same Phase 3 scene wave now also includes MATLAB-style `daylight(...)` plus curated Octave parity on the `s_sceneDaylight.m` workflow, comparing photon and energy daylight SPDs across 4000:1000:10000 K, their per-spectrum luminance normalization to 100 cd/m^2, the CIE daylight basis functions, and the mean / mean+S1 / mean-S1 basis examples.
- That same Phase 3 scene wave now also includes headless `illuminantCreate/Get/Set` for the `s_sceneIlluminant.m` workflow, with curated Octave parity on the default and 3000 K blackbody illuminants, D65/equal-energy/equal-photons/illuminant-C/555 nm construction, D65 wave-resampling through `illuminantSet(..., 'wave', ...)`, and the fluorescent/tungsten illuminant paths.
- That same Phase 3 scene wave now also includes headless `sceneIlluminantSS(...)`, spatial-spectral `sceneAdjustIlluminant(...)`, `sceneGet(..., 'reflectance')`, and `sceneCreate('macbeth tungsten')` for the `s_sceneIlluminantMixtures.m` workflow, with curated Octave parity on the mixed-scene top/bottom illuminant spectra, preserved reflectance, and mean-luminance contract.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneIlluminantSpace.m` workflow, locking down the spatial-spectral conversion, blackbody-varying row illuminants, preserved reflectance after row/column modulation, and the column-harmonic illuminant profile at 550 nm.
- That same scene plotting surface now also includes headless `scenePlot(..., 'illuminant image')`, matching MATLAB's spectral illuminant replication and spatial-spectral illuminant rendering through returned `srgb` payloads without opening a figure.
- That same image-processing surface now also includes MATLAB-style `Demosaic.m` routing for RGB Bayer sensors through `ipSet(..., 'demosaic method', ...)`, including `ieNearestNeighbor`, `Laplacian`, and `AdaptiveLaplacian` with the same bilinear fallback upstream uses for the unsupported `gbrg` adaptive-Laplacian branch.
- That same image-processing surface now also exposes headless `demosaic(...)` / `Demosaic(...)`, matching the tutorial-visible `Demosaic(ip, sensor)` entry point and preferring the current IP `input` mosaic when present.
- That same image-processing surface now also exposes headless `image_sensor_conversion(...)` / `imageSensorConversion(...)` and `image_sensor_correction(...)` / `imageSensorCorrection(...)`, matching the direct MATLAB sensor-conversion entry points on top of the existing IP internal-color-space path.
- That same image-processing surface now also exposes headless `image_illuminant_correction(...)` / `imageIlluminantCorrection(...)`, matching the direct MATLAB ICS white-balance entry point on top of the existing IP illuminant-correction path.
- That same image-processing surface now also exposes headless `image_rgb_to_xyz(...)` / `imageRGB2XYZ(...)`, matching the direct MATLAB display-linear RGB-to-XYZ entry point used by Macbeth and display-plot helpers while preserving RGB or XW data format.
- That same image-processing surface now also exposes headless `display_render(...)` / `displayRender(...)`, matching the direct MATLAB internal-color-space-to-display entry point on top of the existing IP display-render stage.
- That same image-processing surface now also exposes headless `image_sensor_transform(...)` / `imageSensorTransform(...)` and `image_esser_transform(...)` / `imageEsserTransform(...)`, matching the direct MATLAB sensor-to-target linear transform calculators for the multisurface and Esser-optimized training sets.
- That same image-processing surface now also exposes headless `ie_internal_to_display(...)` / `ieInternal2Display(...)`, matching the direct MATLAB internal-color-space to linear-display transform helper used under `displayRender(...)` and related IP workflows.
- That same image-processing surface now also exposes headless `ip_hdr_white(...)` / `ipHDRWhite(...)`, matching the direct MATLAB HDR highlight-whitening helper on top of the current IP `input` and `result` payloads.
- That same image-processing surface now also exposes headless `image_distort(...)` / `imageDistort(...)`, matching the direct MATLAB image-distortion gateway for Gaussian noise, JPEG compression, and simple contrast scaling.
- That same image-processing surface now also exposes headless `ip_clear_data(...)` / `ipClearData(...)`, matching the direct MATLAB computed-payload reset path and the `ipSet(..., 'data', [])` behavior.
- That same image-processing surface now also exposes headless `vcimage_clear_data(...)` / `vcimageClearData(...)`, matching the legacy MATLAB alias for the same computed-payload reset path.
- That same image-processing surface now also exposes headless `image_color_balance(...)` / `imageColorBalance(...)`, matching the deprecated MATLAB alias for the same illuminant-correction stage.
- That same image-processing surface now also exposes headless `vcimage_srgb(...)` / `vcimageSRGB(...)`, matching the legacy MATLAB scene-to-sRGB convenience pipeline and adding MATLAB-style `colorBalanceMethod` / `colorconversionmethod` alias support in `ipSet(...)`.
- That same image-processing surface now also exposes headless `vcimage_iso_mtf(...)` / `vcimageISOMTF(...)`, matching the legacy MATLAB slanted-edge ISO 12233 convenience wrapper on top of the current `camera_mtf(...)` workflow.
- That same image-processing surface now also exposes headless `vcimage_vsnr(...)` / `vcimageVSNR(...)`, matching the legacy MATLAB ROI-based visual-SNR convenience wrapper on top of the current headless VSNR ROI computation.
- That same image-processing surface now also exposes headless `ip_mcc_xyz(...)` / `ipMCCXYZ(...)` and the legacy alias `vcimage_mcc_xyz(...)` / `vcimageMCCXYZ(...)`, matching the Macbeth chart patch-XYZ helper for both `sRGB` and display-model (`custom`) conversion paths.
- That same Phase 3 scene wave now also includes `sceneGet(..., 'xyz')` plus MATLAB-style `RGB2XWFormat` / `XW2RGBFormat` support for the `s_sceneXYZilluminantTransforms.m` workflow, with curated Octave parity on the normalized D65-vs-tungsten XYZ balance, the fitted full and diagonal transfer matrices, and their relative predicted-vs-observed RMSE contract.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneFromRGB.m` workflow, locking down `displayCreate('LCD-Apple.mat')`, the display white chromaticity, the `sceneFromFile(..., 'rgb', ..., 'LCD-Apple.mat')` geometry/luminance path, the preserved mean luminance after the 6500 K illuminant swap, and the selected beak ROI reflectance spectrum.
- That same display compatibility surface now also includes headless `display_list(...)` / `displayList(...)`, `display_description(...)` / `displayDescription(...)`, `display_show_image(...)` / `displayShowImage(...)`, `display_set_max_luminance(...)` / `displaySetMaxLuminance(...)`, `display_set_white_point(...)` / `displaySetWhitePoint(...)`, `display_max_contrast(...)` / `displayMaxContrast(...)`, `ie_calculate_monitor_dpi(...)` / `ieCalculateMonitorDPI(...)`, and `mperdot2dpi(...)`, matching the older MATLAB display utility family without GUI prompts or windows.
- That same Macbeth helper surface now also includes headless `macbeth_rectangles(...)` / `macbethRectangles(...)`, `macbeth_rois(...)` / `macbethROIs(...)`, `macbeth_patch_data(...)` / `macbethPatchData(...)`, and `macbeth_ideal_color(...)` / `macbethIdealColor(...)`, matching the older MATLAB chart-geometry, ROI extraction, patch-statistics, and ideal-target utility family without GUI interaction.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneFromMultispectral.m` workflow, locking down `sceneFromFile(..., 'multispectral', ..., 400:10:700)` on `StuffedAnimals_tungsten-hdrs`, the loaded scene geometry and mean luminance, and the normalized mean and center scene spectra.
- That same Phase 3 scene wave now also includes headless `sceneGet(..., 'rgb')` support for the `s_sceneFromRGBvsMultispectral.m` workflow, with curated Octave parity on the multispectral-to-RGB-to-scene roundtrip through `LCD-Apple.mat`, comparing preserved scene geometry, preserved mean luminance, preserved 6500 K illuminant chromaticity, and channelwise RGB/XYZ roundtrip correlations.
- That same Phase 3 scene wave now also includes headless `ie_reflectance_samples(...)` / `ieReflectanceSamples(...)` for the `s_sceneReflectanceSamples.m` workflow, with curated Octave parity on no-replacement sampling, exact replay through returned sample lists, and deterministic normalized-reflectance mean / singular-value summaries on explicit reflectance selections.
- That same Phase 3 scene wave now also includes headless `hc_basis(...)` / `hcBasis(...)` for the `s_sceneReflectanceChartBasisFunctions.m` workflow, with curated Octave parity on the canonical reflectance-chart basis extraction path at `bType=0.999`, `0.95`, and `5`, including selected basis counts, variance explained, sign-canonicalized basis vectors, and coefficient summary statistics.
- That same Phase 3 scene wave now also includes headless `scene_reflectance_chart(...)` / `sceneReflectanceChart(...)` for the `s_sceneReflectanceCharts.m` workflow, with curated Octave parity on the default Natural-100 chart, the explicit no-replacement Munsell/Food/Dupont/Hyspex chart, the D65 illuminant-adjusted branch, the gray-strip variant, and exact replica-chart replay from the returned stored sample lists.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneChangeIlluminant.m` workflow, locking down the default Macbeth-to-tungsten illuminant swap together with the StuffedAnimals multispectral scene adjusted to equal-energy and Horizon illuminants, with parity on preserved mean luminance, normalized illuminant spectra, and rendered-RGB mean summaries.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneDataExtractionAndPlotting.m` workflow, locking down the default `macbethd65` line/ROI extraction path through `scenePlot(...)`, `ieRect2Locs(...)`, and `vcGetROIData(...)`, with parity on the luminance-hline support/profile, illuminant-energy spectrum, yellow-patch radiance and reflectance ROI spectra, and exact manual-versus-plotted ROI mean agreement.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneMonochrome.m` workflow, locking down `displayCreate('crt')`, `sceneFromFile('cameraman.tif', 'monochrome', 100, 'crt')`, the source display-white illuminant and monochrome scene spectral summaries, and the D65-adjusted unispectral scene plus rendered-RGB mean behavior.
- That same Phase 3 scene wave now also includes upstream-style `scene_plot(..., 'illuminant energy roi')` fallback behavior without an explicit ROI, plus curated Octave parity on the `s_sceneSlantedBar.m` workflow, locking down the equal-photons and D65 illuminant spectra together with the two slanted-bar constructions’ geometry, mean luminance, and normalized center-row/center-column luminance profiles.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneHarmonics.m` workflow, locking down the four exact harmonic constructions from the script: the basic 1 cpi pattern, the two-frequency phase-offset sum, the crossed-orientation 2-and-5 cpi pair, and the symmetric 5-and-5 cpi pair, with parity on scene geometry, preserved mean luminance, and normalized center-row/center-column luminance profiles.
- That same Phase 3 scene wave now also includes headless `sceneCreate('zone plate', ...)` support for the `s_sceneZonePlate.m` workflow, with curated Octave parity on the equal-photon zone-plate generator, its default `4 deg` field of view, and the clipped hyperspectral photon cube.
- That same scene-family wave now also includes headless `sceneCreate('dead leaves', ...)` support, with curated Octave parity on the OLED-Sony display-rendered grayscale target, its default `10 deg` field of view, and the deterministic dead-leaves photon cube produced from a fixed sample stream.
- That same scene-family wave now also includes corrected `sceneCreate('bar', ...)` luminance normalization, with curated Octave parity on the centered three-pixel equal-photon bar scene and its `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes corrected shared `sceneCreate('line ...', ...)` luminance normalization, with curated Octave parity on the `sceneCreate('line ee', ...)`, `sceneCreate('line ep', ...)`, and `sceneCreate('lined65', ...)` vertical-line scenes and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniform monochromatic', ...)`, locking down the single-wave `550 nm` narrow-band scene, its `12x12` geometry, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniform d65', ...)`, locking down the `24x24` D65 uniform scene, its flat spatial layout with non-flat D65 spectrum, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('empty')`, locking down the cleared Macbeth-D65 geometry, preserved illuminant spectrum, all-zero photon cube, and `0 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('lstar', ...)`, locking down the `80x200` stepped L* chart, its monotonically increasing 20-bar luminance sequence, the full photon cube, and the shared `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes corrected `sceneCreate('hdr')` mean-luminance normalization plus curated Octave parity on the default `384x384` HDR-lights constructor, locking down the normalized mean spectrum, sampled normalized luminance rows through the circle/line/square bands, and the returned `100 cd/m^2` contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('macbethtungsten')`, locking down the `64x96` Macbeth chart under tungsten illumination together with the returned `100 cd/m^2` photon-cube contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniform', ...)` / `sceneCreate('uniformEE', ...)`, locking down the direct `24x24` equal-energy constructor alias, its flat spatial layout with wavelength-varying photon spectrum, and the same `100 cd/m^2` luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniform equal photon', ...)`, locking down the `24x24` equal-photon uniform scene, its flat spectral-spatial photon cube, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniformbb', ...)`, locking down the compact blackbody alias and its exact equivalence to `sceneCreate('uniform bb', 16, 4500, ...)`.
- That same scene-family wave now also includes Python `sceneCreate('uniformblackbody', ...)` alias coverage, with curated Octave parity against the canonical upstream `sceneCreate('uniformbb', 16, 4500, ...)` constructor and exact equivalence to `sceneCreate('uniform bb', 16, 4500, ...)`.
- That same scene-family wave now also includes the missing upstream `sceneCreate('uniformephoton')` alias in Python, with curated Octave parity on the compact equal-photon alias and exact equivalence to `sceneCreate('uniform ep', ...)`.
- That same scene-family wave now also includes curated Octave parity on Python `sceneCreate('uniformep', ...)`, using the canonical upstream `sceneCreate('uniformephoton', ...)` constructor and locking exact equivalence back to `sceneCreate('uniform ep', ...)`.
- That same scene-family wave now also includes curated Octave parity on Python `sceneCreate('uniformequalphoton', ...)` and `sceneCreate('uniformequalphotons', ...)`, using the canonical upstream singular `sceneCreate('uniformequalphoton', ...)` constructor and locking exact equivalence back to `sceneCreate('uniform ep', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('line', ...)`, locking down the direct D65 line-scene alias, the shared `64x64` vertical-line photon cube, and its exact equivalence to `sceneCreate('lined65', ...)`.
- That same scene-family wave now also includes curated Octave parity on Python `sceneCreate('lineequalphoton', ...)`, using the upstream `sceneCreate('lineequalphoton', ...)` constructor and locking exact equivalence back to `sceneCreate('line ep', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('lineee', ...)`, locking down the compact equal-energy line alias, the shared `64x64` vertical-line photon cube, and its exact equivalence to `sceneCreate('line ee', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('impulse1dd65', ...)`, locking down the impulse-style D65 line alias, the shared `64x64` vertical-line photon cube, and its exact equivalence to `sceneCreate('lined65', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('impulse1dee', ...)`, locking down the impulse-style equal-energy line alias, the shared `64x64` vertical-line photon cube, and its exact equivalence to `sceneCreate('line ee', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('sinusoid', ...)`, locking down the direct harmonic-scene alias, the shared `64x64` multi-frequency photon cube, and its exact equivalence to `sceneCreate('harmonic', ...)`.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('uniformEESpecify', ...)`, locking down the explicit `380:10:720 nm` equal-energy uniform scene, its `128x128` geometry, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('exponential intensity ramp', ...)`, locking down the `64x64` exponential ramp with dynamic range `256`, its row-uniformity, monotonic column profile, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('linear intensity ramp', ...)`, locking down the `64x64` linear ramp with dynamic range `256`, its per-row monotonic growth, top-row full-scale ratio, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('rings rays', ...)`, locking down the `64x64` Mackay-style radial scene at frequency `8`, its center-row/center-column symmetry, and the same `100 cd/m^2` mean-luminance contract.
- That same scene-family wave now also includes corrected shared `sceneCreate('point array', ...)` / `sceneCreate('grid lines', ...)` luminance normalization, with curated Octave parity on `sceneCreate('point array', ...)` locking down the `64x64` equal-photon point lattice with `16 px` spacing, its exact `4x4` bright-point grid, and the sparse geometry used by downstream optics and sensor workflows, and on `sceneCreate('grid lines', ...)` locking down the matching `64x64` equal-photon distortion grid with `16 px` spacing, exact `4x4` full-bright line support, and the same `100 cd/m^2` luminance contract.
- That same scene-family wave now also includes curated Octave parity on `sceneCreate('white noise', ...)`, locking down the deterministic `128x128` D65 white-noise scene at `20%` contrast through its `1 deg` FOV, photon-cube geometry, normalized spatial distribution summaries, and normalized mean spectrum instead of brittle raw random pixels.
- That same scene-family wave now also includes corrected `sceneCreate('disk array', ...)` mean-luminance normalization plus curated Octave parity, locking down the deterministic `64x64` equal-photon `2x2` disk lattice with radius `8`, its exact four connected components and centroids, the two-level photon cube, and the same `100 cd/m^2` luminance contract.
- That same scene-family wave now also includes `sceneCreate('square array', ...)` plus curated Octave parity, locking down the deterministic `64x64` equal-photon `2x2` square lattice with side `8`, its exact four connected components and centroids, the two-level photon cube, and the same `100 cd/m^2` luminance contract.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_surfaceMunsell.m` workflow, locking down explicit `munsell.mat` loading from `data/surfaces/charts`, the illuminant-C wavelength/support vector, the reshaped XYZ-to-sRGB chip rendering, chromaticity and LAB summary bounds, and the first 45 hue/value/angle records printed by the script.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneDemo.m` workflow, locking down the `macbethd65` scene creation path, luminance/photons/wave extraction with the script’s max-photon and mean-spectrum invariants, the `20 deg` FOV update, and the `freq orient pattern` bottom-row luminance plus `550 nm` radiance-line summaries.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneExamples.m` workflow, locking down the built-in example scene catalog across `rings rays`, frequency-orientation, harmonic, checkerboard, line, slanted bar, grid, point-array, Macbeth tungsten, `uniformEESpecify`, `lstar`, and exponential-ramp scenes, with deterministic center-row/center-column luminance summaries and geometry-only coverage for the default random reflectance chart.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneRoi.m` workflow, locking down ROI photons/energy/illuminant/reflectance extraction on the default scene, the returned ROI-mean spectra, and exact manual-versus-direct reflectance agreement.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneRotate.m` workflow, locking down the default star-pattern rotation movie through selected rotated-frame sizes, luminance summaries, and canonicalized center-row/center-column luminance profiles.
- That same Phase 3 scene wave now also includes curated Octave parity on the `s_sceneWavelength.m` workflow, locking down default-scene wavelength resampling through `sceneSet(..., 'wave', ...)`, preserved geometry/luminance at 10 nm, 5 nm, and 2 nm narrowband supports, and the normalized mean/center spectral-radiance trends across those three scene states.
- That same Phase 3 scene wave now also includes headless `ie_save_multispectral_image(...)` / `ieSaveMultiSpectralImage(...)` for the `s_sceneHCCompress.m` workflow, with curated Octave parity on the 95% and 99% `hcBasis(...)` compression roundtrip of `StuffedAnimals_tungsten-hdrs`, including the basis counts, save/reload geometry, and reconstructed 5 nm mean/center spectral trends.
- That same Phase 3 scene wave now also includes headless `image_increase_image_rgb_size(...)` / `imageIncreaseImageRGBSize(...)` for the `s_sceneIncreaseSize.m` workflow, with curated Octave parity on the exact `[2 3]`, `[1 2]`, and `[3 1]` pixel-replication path, preserved mean luminance and normalized mean-SPD trends after each enlargement, and exact replay of each enlarged photon cube back to the previous state through stride-based decimation.
- That same Phase 3 scene wave now also includes headless `scene_show_image(...)` / `sceneShowImage(...)`, Haar-path `hdr_render(...)` / `hdrRender(...)`, and MATLAB-style column-vector illuminant support in `sceneAdjustIlluminant(...)` for the `s_sceneRender.m` workflow, with curated Octave parity on the D75-adjusted StuffedAnimals render plus the Feng Office and StuffedAnimals HDR-companded render summaries, center RGB samples, and canonicalized center-row luminance profiles.
- That same Phase 3 scene wave now also includes headless `scene_radiance_from_vector(...)` / `sceneRadianceFromVector(...)`, `scene_photons_from_vector(...)` / `scenePhotonsFromVector(...)`, and `scene_energy_from_vector(...)` / `sceneEnergyFromVector(...)`, matching the direct MATLAB vector-to-scene-cube helper mini-family used by older scene setup code.
- That same Phase 3 scene wave now also includes headless `scene_init_geometry(...)` / `sceneInitGeometry(...)`, `scene_spatial_resample(...)` / `sceneSpatialResample(...)`, and `scene_photon_noise(...)` / `scenePhotonNoise(...)`, extending the legacy MATLAB scene geometry/support/export helper surface on top of the existing `sceneGet(...)`, ROI, and `sceneShowImage(...)` paths.
- That same Phase 3 scene wave now also includes headless `scene_add_grid(...)` / `sceneAddGrid(...)`, `scene_adjust_pixel_size(...)` / `sceneAdjustPixelSize(...)`, `scene_illuminant_scale(...)` / `sceneIlluminantScale(...)`, `scene_spd_scale(...)` / `sceneSPDScale(...)`, and `scene_adjust_reflectance(...)` / `sceneAdjustReflectance(...)`, together with the remaining `sceneGet/Set` metadata needed for `sample spacing`, `energy`, `known reflectance`, and `peak radiance and wave`.
- That same Phase 3 scene wave now also includes headless `FOTParams(...)`, `gaborP(...)`, `ieCheckerboard(...)`, `MOTarget(...)`, and `sceneRamp(...)`, plus direct `sceneCreate('ramp', ...)` support for the lightweight MATLAB scene-pattern helper layer already backed by the existing frequency-orientation, harmonic, checkerboard, and ramp generators.
- That same Phase 3 scene wave now also includes the legacy `scene/imgtargets` grayscale helper layer: `img_deadleaves(...)` / `imgDeadleaves(...)`, `img_disk_array(...)` / `imgDiskArray(...)`, `img_mackay(...)` / `imgMackay(...)`, `img_radial_ramp(...)` / `imgRadialRamp(...)`, `img_ramp(...)` / `imgRamp(...)`, `img_square_array(...)` / `imgSquareArray(...)`, `img_sweep(...)` / `imgSweep(...)`, and `img_zone_plate(...)` / `imgZonePlate(...)`, all routed through the same underlying pattern generators already used by the scene constructors.
- The same sensor/simulation compatibility wave now also includes headless `sensorComputeNoiseFree(...)`, `sensorAddNoise(...)`, `sensorComputeImage(...)`, and `sensorComputeFullArray(...)`, matching the legacy MATLAB sensor-simulation wrapper layer on top of the existing Python sensor compute path.
- That same Phase 3 scene wave now also includes headless `scene_description(...)` / `sceneDescription(...)`, `scene_list(...)` / `sceneList(...)`, and `scene_thumbnail(...)` / `sceneThumbnail(...)`, matching the legacy MATLAB scene description/listing/thumbnail helper mini-family on top of the existing headless scene render path.
- That same Phase 3 scene wave now also includes headless `scene_crop(...)` / `sceneCrop(...)`, `scene_extract_waveband(...)` / `sceneExtractWaveband(...)`, and `scene_translate(...)` / `sceneTranslate(...)`, matching the legacy MATLAB scene crop, waveband-extraction, and translation helper mini-family.
- That same Phase 3 scene wave now also includes MATLAB-style upstream RGB asset lookup in `scene_from_file(..., 'rgb', ...)` for the `t_sceneRGB2Radiance.m` workflow, with curated Octave parity on `sceneFromFile('macbeth.tif', 'rgb', ..., display)` across `OLED-Sony`, `LCD-Apple`, and `CRT-Dell`, comparing the display white points, primary chromaticities, scene luminance/SPD summaries, and display-dependent rendered RGB means.
- That same Phase 3 scene wave now also includes headless `macbeth_read_reflectance(...)` / `macbethReadReflectance(...)`, `xyz_to_srgb(...)` / `xyz2srgb(...)`, and `image_flip(...)` / `imageFlip(...)` for the `t_sceneSurfaceModels.m` tutorial, with curated Octave parity on the Macbeth reflectance SVD basis path, the 1-D through 4-D reconstruction error trend, normalized D65 rendering, and the rendered chart RGB summaries for the reduced and full basis cases.
- The post-metrics color-script wave now also includes curated Octave parity on `s_reflectanceBasis.m`, covering the upstream reflectance-file enumeration, the `[5, 12]` two-file aggregate reflectance matrix on `400:5:700`, the 8-D SVD basis subspace, the first four sign-canonicalized basis vectors, and the reduced-dimensional reconstruction RMSE/statistics.
- That same color-script wave now also includes curated Octave parity on `s_colorIlluminantTransforms.m`, covering the blackbody `3500:500:8000 K` transform bank on the default reflectance chart, the unit-length vectorized 3x3 transform table, and the Buddha/flower cosine-similarity tables used by the script.
- That same color-script wave now also includes curated Octave parity on `s_chromaticSpatialChart.m`, covering the synthesized row-chromatic / column-frequency RGB chart, the white-border assembly, the `sceneFromFile(..., 'rgb', 100, 'LCD-Apple')` conversion, and the normalized scene luminance/spectrum center-line summaries.
- That same color-script wave now also includes curated Octave parity on `s_colorConstancy.m`, covering the stuffed-animals spectral scene and the `uniformD65` scene under the script's 15 reciprocal-temperature blackbody sweep, with parity on the per-frame mean luminance plus normalized rendered-RGB mean and center summaries.
- That same color-script wave now also includes headless `srgb_to_color_temp(...)` / `srgb2colortemp(...)` plus curated Octave parity on `s_rgbColorTemperature.m`, covering the Macbeth tungsten/D65 camera pipeline, the returned coarse CCT bucket from the script's `2500:500:10500 K` lookup table, the shared lookup-table chromaticities, and the normalized rendered-RGB mean summaries.
- That same color-script wave now also includes headless `scene_reflectance_chart(...)` / `sceneReflectanceChart(...)` plus `srgb_parameters(...)` / `adobergb_parameters(...)`, with curated Octave parity on `s_srgbGamut.m`, covering the natural and synthetic explicit-surface charts, the closed sRGB/Adobe RGB gamut polygons, and the D65 / 3000 K chromaticity clouds derived from the script's reflectance sets.
- That same color compatibility surface now also includes headless `ie_xyz_from_photons(...)` / `ieXYZFromPhotons(...)`, `ie_luminance_to_radiance(...)` / `ieLuminance2Radiance(...)`, `ie_scotopic_luminance_from_energy(...)` / `ieScotopicLuminanceFromEnergy(...)`, `ie_responsivity_convert(...)` / `ieResponsivityConvert(...)`, `srgb_to_lrgb(...)` / `srgb2lrgb(...)`, `lrgb_to_srgb(...)` / `lrgb2srgb(...)`, `y_to_lstar(...)` / `Y2Lstar(...)`, `xyy_to_xyz(...)` / `xyy2xyz(...)`, `ie_lab_to_xyz(...)` / `ieLAB2XYZ(...)`, and the direct Stockman transform helpers `xyz_to_lms(...)` / `xyz2lms(...)`, `lms_to_xyz(...)` / `lms2xyz(...)`, and `lms_to_srgb(...)` / `lms2srgb(...)`, matching the remaining direct MATLAB color-transform helper mini-family.
- That same color compatibility surface now also includes headless `cct_to_sun(...)` / `cct2sun(...)`, `ie_ctemp_to_srgb(...)` / `ieCTemp2SRGB(...)`, `ie_circle_points(...)` / `ieCirclePoints(...)`, and `mk_inv_gamma_table(...)` / `mkInvGammaTable(...)` for the legacy MATLAB color-temperature, circle-sampling, and inverse-gamma helper mini-family.
- The script-driven raw sensor/tutorial path now includes headless DNG support through `ieDNGRead`, `ieDNGSimpleInfo`, and `sensorDNGRead`, covering the checked-in Pixel 4a `MCC-centered.dng` workflow from `t_sensorReadRaw.m`, the upstream-backed `sensorCreate('IMX363')` crop flow, headless `sensorPlotLine` coverage for the `t_sensorSpatialResolution.m` line-profile path including the exact `sensorSet(..., 'pixel size Constant Fill Factor', ...)` tutorial call and curated parity on the coarse/fine sensor-vs-OI line data, curated parity on the returned summary-table fields from the `t_sensorFPN.m` `sensorDescription(...)` setup, direct curated parity on the four `noise flag` modes and two-line volts plot from the rest of `t_sensorFPN.m`, `signalCurrent` coverage for the `t_sensorInputRefer.m` current-to-electrons path, headless `SignalCurrentDensity(...)` and `spatialIntegration(...)` coverage for the adjacent legacy sensor-simulation current-density and default grid-integration helpers, direct `analog2digital` / `noiseFPN` / `noiseColumnFPN` coverage for the same legacy quantization and fixed-pattern-noise helper surface, direct `sensorRGB2Plane` / `sensorStats` / `sensorCheckArray` coverage for the adjacent MATLAB CFA-packing, ROI-summary, and CFA-visibility helper layer, direct curated parity on the ROI-DV summary statistics workflow from `s_sensorPoissonNoise.m`, `ieReadSpectra` coverage for the Macbeth/D65/scanner spectral-loading path from `t_sensorEstimation.m`, direct curated parity on the daylight-basis Macbeth illuminant solve from `s_sensorMacbethDaylightEstimate.m`, `sensorColorFilter` / `ieReadColorFilter` / `ieSaveColorFilter` coverage for the Gaussian color-filter roundtrip workflow from `s_sensorGaussianFilter.m`, direct curated parity on calibrated `ieReadColorFilter('NikonD100')` asset loading from `s_sensorPlotColorFilters.m`, direct curated parity on the simulated filter-responsivity and spectral-filter recovery workflow from `s_sensorSpectralEstimation.m`, direct curated parity on the CFA construction workflow from `s_sensorCFA.m`, now including the full zebra-scene camera workflow plus MATLAB-style `cameraSet(..., 'pixel size constant fill factor', ...)` routing, exact branch pattern/size metadata, and normalized sensor-RGB summaries across the default Bayer, explicit Bayer, YCMY, custom 3x3 RGB, RGBW, and quad-CFA branches, direct curated parity on the CFA-matched exposure-duration workflow from `s_sensorExposureCFA.m`, curated parity on the cropped CFA point-image sweep from `s_sensorCFAPointSpread.m`, `filter transmissivities` and overexposed `ipCompute` color-shift coverage for `t_sensorExposureColor.m`, direct curated parity on the stacked multi-exposure workflow from `s_sensorExposureBracket.m`, `ieFitLine`-driven dark-voltage estimation coverage for `s_sensorAnalyzeDarkVoltage.m`, slope-based PRNU estimation coverage for `s_sensorSpatialNoisePRNU.m`, DSNU estimation coverage for `s_sensorSpatialNoiseDSNU.m`, direct curated parity for the sensor-noise component curves returned by `sensorSNR(...)` in `s_sensorSNR.m`, the photon-counting workflow from `s_sensorCountingPhotons.m`, including MATLAB-style `sceneCreate('uniform equal photon', [rows cols])`, `sceneSet(..., 'mean luminance', ...)`, and `oiGet(..., 'optics aperture diameter', unit)`, and headless `sensor_formats(...)` / `sensorFormats(...)` plus `ie_n_to_megapixel(...)` / `ieN2MegaPixel(...)` for the size-versus-megapixel sweep in `s_sensorSizeResolution.m`.
- That same sensor compatibility surface now also includes headless `sensor_mpe30(...)` / `sensorMPE30(...)`, `sensor_pd_array(...)` / `sensorPDArray(...)`, `sensor_jiggle(...)` / `sensorJiggle(...)`, and `sensor_wb_compute(...)` / `sensorWBCompute(...)`, matching the remaining MATLAB SNR-threshold, photodetector-coverage, no-op motion, and per-waveband sensor replay helpers on top of the existing SNR, pixel, MAT-object load, and sensor-compute paths.
- That same sensor/simulation compatibility surface now also includes headless `bin_noise_fpn(...)` / `binNoiseFPN(...)`, `bin_noise_column_fpn(...)` / `binNoiseColumnFPN(...)`, and `bin_noise_read(...)` / `binNoiseRead(...)`, matching the legacy MATLAB binning-path DSNU/PRNU, column-FPN, and read-noise helpers on top of the existing fixed-pattern and read-noise simulation paths; the upstream `noiseDarkCurrent.m` file is now explicitly classified as out of scope because it immediately errors as obsolete.
- That same sensor wave now also includes headless `sensorCreateArray`, `sensorCreateSplitPixel`, and `sensorComputeArray` support for the OVT split-pixel saturated-combine path that underlies `s_sensorSplitPixel.m`, with curated Octave parity on the combined-volts output and saturated-pixel metadata counts.
- That same sensor wave now also includes sensor-array `sensorCompute(...)` support plus `ieReadSpectra('Foveon'/'NikonD1')` sensor-color-filter asset loading for the stacked monochrome-plane/triple-well workflow from `s_sensorStackedPixels.m`, with curated parity on the stable stacked-plane summaries and the Bayer comparison-line summaries.
- That same Phase 1 sensor wave now also includes direct `mlensCreate`, `mlensSet`, `mlensGet`, `mlRadiance`, and `mlAnalyzeArrayEtendue` support for the microlens-array workflow from `s_sensorMicrolens.m`, with curated Octave parity on the no-microlens/centered/optimal etendue maps, the optimal-offset curve and offset-map variants, and the single-pixel irradiance midlines used by the upstream chief-ray-angle illustration.
- That same microlens compatibility surface now also includes headless `ml_get_current(...)` / `mlGetCurrent(...)`, `ml_set_current(...)` / `mlSetCurrent(...)`, `ml_import_params(...)` / `mlImportParams(...)`, `ml_description(...)` / `mlDescription(...)`, and `ml_print(...)` / `mlPrint(...)`; the remaining GUIDE microlens window/controller files are now explicitly classified as out of scope in the migration audit rather than actionable headless API gaps.
- That same Phase 1 sensor wave now also includes MATLAB-style `sceneSet(..., 'resize', ...)`, public `sceneCombine(...)`, and `sensorSet(..., 'hfov'/'vfov', ...)` support for the mixed-sensor comparison workflow from `s_sensorComparison.m`, with curated Octave parity on the combined-scene geometry and the IMX363 / MT9V024 RCCC / CYYM small-pixel versus large-pixel response summaries.
- That same Phase 1 sensor wave now also includes headless `sensorComputeSamples(...)` for the repeated-capture workflow from `s_sensorNoise.m`, with curated Octave parity on stable sample-stack noise statistics rather than raw RNG-matched capture volumes.
- That same sensor compatibility surface now also includes headless `sensor_clear_data(...)` / `sensorClearData(...)`, matching the direct MATLAB computed-payload reset path for sensor objects.
- That same sensor compatibility surface now also includes headless `sensor_show_image(...)` / `sensorShowImage(...)`, matching the direct MATLAB sensor-window render entry point through the existing `dv or volts` RGB conversion path.
- That same sensor compatibility surface now also includes headless `sensor_save_image(...)` / `sensorSaveImage(...)`, matching the direct MATLAB PNG-export helper on top of the existing sensor RGB conversion path.
- That same sensor compatibility surface now also includes headless `sensor_show_cfa(...)` / `sensorShowCFA(...)`, matching the direct MATLAB CFA-pattern render helper on top of the existing sensor RGB conversion path.
- That same sensor compatibility surface now also includes headless `sensor_image_color_array(...)` / `sensorImageColorArray(...)`, matching the legacy MATLAB CFA-letter to color-order map helper used by the older sensor CFA utilities.
- That same sensor compatibility surface now also includes headless `sensor_show_cfa_weights(...)` / `sensorShowCFAWeights(...)`, matching the weighted CFA visualization helper used by the same legacy sensor CFA utilities.
- That same sensor compatibility surface now also includes headless `sensor_color_order(...)` / `sensorColorOrder(...)` and `sensor_determine_cfa(...)` / `sensorDetermineCFA(...)`, matching the legacy CFA color-hint and tiled-pattern helpers used across the same older sensor CFA utilities.
- That same sensor compatibility surface now also includes headless `sensor_snr_luxsec(...)` / `sensorSNRluxsec(...)`, matching the direct MATLAB lux-sec sensor-SNR helper on top of the existing `sensorSNR(...)` / `pixelVperLuxSec(...)` path.
- That same sensor compatibility surface now also includes headless `sensor_display_transform(...)` / `sensorDisplayTransform(...)`, `sensor_equate_transmittances(...)` / `sensorEquateTransmittances(...)`, and `sensor_filter_rgb(...)` / `sensorFilterRGB(...)`, matching the direct MATLAB sensor-channel display-transform, equal-area filter-normalization, and CFA filter-color helper mini-family.
- That same sensor compatibility surface now also includes headless `sensor_cfa_name_list(...)` / `sensorCFANameList(...)` and `sensor_pixel_coord(...)` / `sensorPixelCoord(...)`, matching the legacy CFA-popup label list and direct MATLAB pixel-center coordinate helper; the migration-gap audit now also treats the explicitly obsolete upstream `sensorUnitBlock.m` stub as out of scope rather than a remaining headless API gap.
- That same sensor compatibility surface now also includes headless `sensor_no_noise(...)` / `sensorNoNoise(...)`, `sensor_gain_offset(...)` / `sensorGainOffset(...)`, and `sensor_resample_wave(...)` / `sensorResampleWave(...)`, matching the legacy MATLAB sensor noise-reset, analog gain/offset, and spectral-resampling helper mini-family.
- That same sensor compatibility surface now also includes headless `sensor_rescale(...)` / `sensorRescale(...)`, `sensor_cfa_save(...)` / `sensorCfaSave(...)`, and `sensor_from_file(...)` / `sensorFromFile(...)`, matching the legacy MATLAB sensor geometry/CFA-persistence/file-load helper mini-family.
- That same sensor compatibility surface now also includes headless `pixel_create(...)` / `pixelCreate(...)`, `pixel_get(...)` / `pixelGet(...)`, `pixel_set(...)` / `pixelSet(...)`, `pixel_ideal(...)` / `pixelIdeal(...)`, `pixel_sr(...)` / `pixelSR(...)`, and `ie_pixel_well_capacity(...)` / `iePixelWellCapacity(...)`, matching the legacy standalone pixel helper mini-family built on top of the existing internal sensor-pixel state.
- That same sensor compatibility surface now also includes headless `pixel_position_pd(...)` / `pixelPositionPD(...)`, `pixel_center_fill_pd(...)` / `pixelCenterFillPD(...)`, and `pixel_description(...)` / `pixelDescription(...)`, matching the legacy photodetector-positioning and pixel-summary helper layer built on top of the same standalone/internal sensor-pixel state.
- That same sensor compatibility surface now also includes headless `pixel_transmittance(...)` / `pixelTransmittance(...)` together with `pt_snells_law(...)` / `ptSnellsLaw(...)`, `pt_reflection_and_transmission(...)` / `ptReflectionAndTransmission(...)`, `pt_interface_matrix(...)` / `ptInterfaceMatrix(...)`, `pt_propagation_matrix(...)` / `ptPropagationMatrix(...)`, `pt_scattering_matrix(...)` / `ptScatteringMatrix(...)`, `pt_poynting_factor(...)` / `ptPoyntingFactor(...)`, and `pt_transmittance(...)` / `ptTransmittance(...)`, matching the legacy buried-photodetector dielectric-stack transmission helper family; the migration-gap audit now also treats the legacy `pixelGeometryWindow.m` / `pixelOEWindow.m` figure controllers as out of scope rather than remaining headless API debt.
- That same sensor compatibility surface now also includes headless `sensor_read_color_filters(...)` / `sensorReadColorFilters(...)`, `sensor_read_filter(...)` / `sensorReadFilter(...)`, `sensor_add_filter(...)` / `sensorAddFilter(...)`, `sensor_replace_filter(...)` / `sensorReplaceFilter(...)`, and `sensor_delete_filter(...)` / `sensorDeleteFilter(...)`, matching the legacy sensor filter-management helper mini-family on top of the existing filter spectra and asset-loading paths.
- That same Phase 1 sensor wave now also includes public `sceneRotate(...)` and `oiCrop(...)` support for the deterministic rolling-shutter assembly workflow from `s_sensorRollingShutter.m`, with curated Octave parity on the temporal sensor trace, the final assembled row summaries, and normalized final RGB summaries.
- That same Phase 1 sensor wave now also includes `sensorCreate('imx490-large')`, `sensorCreate('imx490-small')`, `oiCrop(..., 'border')`, `oiSpatialResample(...)`, and headless `imx490Compute(...)` support for the HDR four-capture combine workflow from `s_sensorIMX490.m`, with curated Octave parity on the deterministic uniform-field best-SNR path, including capture names, capture mean electrons/volts/DV, combined-volts summaries, and selected pixel-source counts.
- That same Phase 1 sensor wave now also includes `sceneFromFile(..., 'multispectral', ...)` support for basis-coded HDR scene assets plus the mixed pixel-size monochrome workflow from `s_sensorHDR_PixelSize.m`, with curated Octave parity on the Feng Office HDR scene path, comparing the three sensor sizes, mean/p95 volts, mean electrons, and grayscale output summaries for the 1/2/4 micron pixel-size sweep.
- That same Phase 1 sensor wave now also includes `sceneCreate('linear intensity ramp')`, `sceneCreate('exponential intensity ramp')`, public `sensor_dr` / `sensorDR`, and headless log-response sensor compute support for `s_sensorLogAR0132AT.m`, with curated Octave parity on the stable noise-free log-response line-shape workflow and the script's 1-second dynamic-range check.
- That same Phase 1 sensor wave now also includes the full `s_sensorAliasing.m` workflow, with curated Octave parity on the stable sweep-frequency aliasing line profiles for the fine-pixel, coarse-pixel, and blurred-lens cases plus canonicalized slanted-bar sensor captures for the sharp and anti-aliased large-pixel paths.
- That same Phase 1 sensor wave now also includes the asset-driven DUT setup workflow from `s_sensorExternalAnalysis.m`, with curated Octave parity on the configured wave/filter/QE payload, CFA pattern, pixel metadata, and imported `dutData.mat` voltage summaries.
- That same Phase 1 sensor wave now also includes the spectral-radiometer workflow from `s_spectralRadiometer.m`, with curated Octave parity on the radiometer filter geometry, deterministic noise-free electron line, theoretical shot-noise curve, and stable photon-noise summary statistics.
- Phase 2 optics coverage now also includes direct `opticsCoC(...)` support for the thin-lens circle-of-confusion workflow from `s_opticsCoC.m`, with curated Octave parity on the 50 mm F/2 and F/8 distance sweeps at 0.5 m and 3 m focus.
- That same Phase 2 optics wave now also includes the wide-field `s_opticsCos4th.m` vignetting workflow, with curated Octave parity on the default and 4x-focal-length illuminance hline profiles plus their mean illuminance summaries for a uniform D65 scene at 80 degrees FOV.
- That same optics wave now also includes MATLAB-style `sceneInterpolateW(...)` behavior through `sceneSet(..., 'wave', ...)`, plus direct Octave-backed parity for the `s_opticsGaussianPSF.m` point-array workflow on the normalized blurred center row and column profiles at 450 nm, 550 nm, and 650 nm.
- That same Phase 2 optics wave now also includes the diffraction-limited `s_opticsPSFPlot.m` workflow, with direct Octave-backed parity on the 600 nm, `f/#=12`, `nSamp=100` PSF surface and Airy-disk radius returned by the headless PSF data contract.
- That same optics wave now also includes the `s_opticsPadCrop.m` workflow, with direct Octave-backed parity on the padded OI geometry, MATLAB crop rectangle, cropped OI geometry, and deterministic sensor line-profile outputs for the scene-FOV and padded-FOV branches.
- That same Phase 2 optics wave now also includes the `s_opticsMicrolens.m` workflow, with direct Octave-backed parity on the microlens getter/setter contract, default 30-degree sensor FOV setup, and the radiance-generated source/pixel irradiance center-line outputs plus etendue.
- That same Phase 2 optics wave now also includes the `s_opticsFlare.m` workflow, with upstream-style `sceneCreate('hdr')` coverage through the HDR-lights scene family, deterministic seeded flare apertures for the Python parity path, and curated Octave parity on the dirty-aperture summaries, normalized 550 nm PSF rows, blur-width statistics, and returned point/HDR OI geometry.
- That same Phase 2 optics wave now also includes the `s_opticsFlare2.m` workflow, with curated Octave parity on the stable dirty-aperture script contract: aperture sums, point-scene blur-width summaries, HDR-scene geometry, and normalized HDR irradiance ratios across the six-sided, five-sided, and defocused branches.
- That same raw DNG path now has curated Octave parity on the cropped `sensorDNGRead(...)+ipCompute(...)` flow, with exact `digital_values` agreement and a bounded normalized-MAE comparator on the rendered RGB result to absorb the remaining legacy display-gain mismatch between the Python and Octave render paths.
- Script-driven shift-invariant optics coverage now also includes `oiCreate('psf')`, including the default synthetic PSF constructor path and explicit custom `psf` / `wave` / `umPerSamp` PSF data.
- The same shift-invariant optics surface now also includes `opticsPSF2OTF` plus `oiSet('optics otfstruct', ...)` for MATLAB-style custom OTF injection from flare PSFs, with a curated parity case that treats the final `photons` field scale-invariantly because upstream Octave preserves image shape but returns a constant-factor magnitude offset.
- The adjacent custom shift-invariant file workflow now also includes `ieSaveSIDataFile(...)` plus direct `siSynthetic('custom', file)` parity, with the case comparing the saved input PSF row directly and treating the downstream OI `photons` field scale-invariantly for the same stable custom-OTF magnitude offset.
- Script-driven WVF coverage now also includes MATLAB-style `wvfSet` / `wvfGet`, `wvfCompute`, defocus microns/diopters conversion helpers, `wvf2oi`, direct `oiCompute(wvf, scene)` support, `oiGet(..., 'wvf ...')`, and rebuilding `oiSet(..., 'wvf ...')` / `oiSet(..., 'optics wvf', ...)` for named Zernike workflows.
- That same WVF-to-OI bridge now also has direct Octave parity on the `s_wvfOI.m` PSF-slice workflow, comparing both `wvfGet(..., 'psf xaxis', ...)` and `wvfGet(..., 'psf yaxis', ...)` against `oiGet(wvf2oi(wvf), 'optics psf xaxis'/'optics psf yaxis', ...)` at 550 nm.
- That same explicit WVF surface now also includes MATLAB-style `wvfPupilFunction` / `wvfComputePSF`, including stored-aperture support and a direct parity case on the standalone pupil-function/PSF workflow.
- That same explicit WVF helper surface now also includes `psf_to_zcoeff_error` / `psf2zcoeff`, plus unit-aware/index-aware `wvfGet(..., 'wave', unit, idx)`, unit-aware `wvfGet(..., 'measured wavelength', unit)`, and MATLAB-style `wvfGet(..., 'z pupil diameter')` support for the upstream `s_opticsPSF2Zcoeffs.m` workflow.
- The same script-driven WVF surface now also includes MATLAB-style `wvfAperture` / `wvfApertureP`, with deterministic clean polygon support and direct Octave parity on the clean-aperture path used by the upstream flare scripts.
- The same script-driven WVF surface now also includes `wvfLoadThibosVirtualEyes`, with the MATLAB default mean-vector return plus explicit covariance/subject-coefficient outputs for the upstream pupil-size scripts.
- That same pupil-size WVF surface now also supports MATLAB key/value `wvfCreate(...)` construction together with `lcaMethod='human'`, with direct Octave parity on the Thibos pupil-size compute path.
- That same pupil-size WVF surface now also has direct Octave parity on the measured-vs-calculated pupil comparison path from `s_wvfPupilSize.m`.
- The same script-driven WVF surface now also supports `wvfSet(..., 'psf sample spacing', ...)`, with direct Octave parity on the `s_wvfPSFSpacing.m` field-size/sampling path.
- That same script-driven WVF helper surface now also includes headless `wvf_to_psf(...)` / `wvf2PSF(...)`, `wvf_to_optics(...)` / `wvf2optics(...)`, and `wvf_pupil_amplitude(...)` / `wvfPupilAmplitude(...)`, matching the direct MATLAB PSF/optics conversion helpers on top of the existing WVF compute path.
- That same script-driven WVF surface now also has direct Octave parity on the defocus/vertical-astigmatism sweep from `s_wvfAstigmatism.m`, comparing the returned normalized PSF center rows and columns at 550 nm across the full 3x3 Zernike grid.
- That same Phase 2 wavefront block now also includes `ieMvnrnd` together with direct Octave parity on the `s_wvfThibosModel.m` workflow, comparing deterministic virtual-eye coefficient draws, the sample-mean subject PSFs across 450/550/650 nm, and the selected example-subject PSF center rows at 450 nm and 550 nm.
- That same Phase 2 wavefront block now also includes direct Octave parity on the `s_wvfZernikeSet.m` workflow, comparing the defocus-plus-vertical-astigmatism loop through the returned Zernike coefficients, 550 nm PSF center rows, and uncropped 550 nm OI center rows for `A = [-1, 0, 1]` at `D = 2` microns.
- That same script-driven WVF surface now also has direct Octave parity on the diffraction workflow from `s_wvfDiffraction.m`, comparing the initial Airy-disk match and `wvf2oi` f-number handoff, the pupil-size sweeps at 550 nm and 400 nm, the human-LCA wavelength sweep, and the focal-length `um per degree` scaling.
- That same script-driven WVF helper surface now also includes headless `sce_create(...)` / `sceCreate(...)` and `sce_get(...)` / `sceGet(...)`, plus direct `wvfGet(..., 'sce'/'sce rho'/'scex0'/'scey0'/'sce wavelengths')` support for the standalone Stiles-Crawford parameter helpers used by the legacy wavefront toolbox.
- That same script-driven WVF helper surface now also includes headless `wvf_root_path(...)` / `wvfRootPath(...)`, `wvf_summarize(...)` / `wvfSummarize(...)`, and `wvf_print(...)` / `wvfPrint(...)`, matching the legacy toolbox-location and text-summary helpers on top of the same WVF core metadata.
- That same explicit WVF compute surface now also supports the MATLAB key/value workflow `wvfCompute(wvf, 'aperture', aperture)`, with direct Octave parity on the polygon-aperture compute path used by the upstream flare scripts.
- The same script-driven optics surface now also includes headless `oiPlot(..., 'psf'/'psf550'/'psfxaxis'/'psfyaxis')`, backed by computed `oiGet(..., 'psf data'/'psf xaxis'/'psf yaxis')` support for diffraction-limited, custom-PSF, and computed WVF shift-invariant optics.
- The adjacent diffraction-limited optics plotting surface now also includes headless `oiPlot(..., 'ls wavelength'/'lswavelength'/'otf wavelength'/'mtf wavelength')` support for the MATLAB line-spread and OTF-by-wavelength workflows used by the diffraction PSF scripts.
- That same diffraction plotting surface now also includes direct `oiPlot(..., 'psf', [], 550)` parity coverage for the upstream `s_opticsDLPsf.m` workflow.
- That same diffraction plotting surface now also includes the script-level `s_opticsDiffraction.m` workflow, with direct Octave parity on the default-to-`f/12` diffraction-limited point-array path, including unit-aware `oiGet(..., 'optics focal length'/'optics pupil diameter', ...)` checks, the returned `psf 550` surface, the `ls wavelength` payload, and stable 550 nm image-blur width summaries.
- The same script-driven custom optics surface now also includes direct `opticsPSF2OTF(...)` parity coverage for the upstream flare-image workflow in `s_opticsPSF2OTF.m`.
- The adjacent `s_wvfOI.m` bridge is now covered on the direct OTF path too: `wvf2oi(...)`-backed OIs can synthesize `oiGet(..., 'optics otf')` from the stored wavefront, and curated parity checks that against `wvfGet(..., 'otf')` after the same `ifftshift` relationship used by the MATLAB script.
- The adjacent wavefront plotting surface now also includes headless `wvfPlot(...)` support for script-driven PSF, 1D PSF, pupil amplitude, pupil phase, wavefront-aberration, PSF-angle, and OTF inspection, backed by wavelength-aware `wvfGet(...)` support for `psf`, `pupil function`, `wavefront aberrations`, `psf spatial samples`, `psf angular samples`, `pupil spatial samples`, `1d psf`, and OTF support.
- That same WVF plotting surface now also has direct Octave parity on the `wvfPlot(..., '2d otf', ...)` workflow from `s_wvfPlot.m`, comparing the returned OTF support axis and center-row magnitude data.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d otf normalized', ...)`, comparing the returned OTF support axis and normalized center-row magnitude data.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d otf', ...)`, comparing the returned cropped OTF support axis and OTF center row.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d otf normalized', ...)`, comparing the returned cropped OTF support axis and normalized OTF center row.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d otf angle', ...)`, comparing the returned cropped frequency support axis and OTF center row.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d otf angle normalized', ...)`, comparing the returned cropped frequency support axis and normalized OTF center row.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d psf space', ...)`, comparing the returned spatial support axis and 1D PSF line data.
- That same WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d wavefront aberrations space', ...)`, comparing the returned spatial support axis and wavefront-aberration center row.
- The shift-invariant optics script coverage now also has direct Octave parity on the circular Gaussian `siSynthetic('gaussian', ..., xyRatio=1)` workflow from `s_opticsSIExamples.m`.
- The same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image pupil amp', ...)`, comparing the returned spatial support axis and pupil-amplitude center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d pupil amplitude space', ...)`, comparing the returned spatial support axis and pupil-amplitude center row for the exact legacy alias.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image pupil phase', ...)`, comparing the returned spatial support axis and pupil-phase center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d pupil phase space', ...)`, comparing the returned spatial support axis and pupil-phase center row for the exact legacy plot name used by the upstream Zernike tutorial.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image wavefront aberrations', ...)`, comparing the returned spatial support axis and wavefront-aberration center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image psf', ...)`, comparing the returned spatial support axis and PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image psf', ..., 'airy disk', true)`, comparing the returned spatial support axis, PSF center row, and Airy-disk radius.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image psf normalized', ...)`, comparing the returned spatial support axis and normalized PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf', 'unit', 'mm', ...)`, comparing the returned cropped spatial support axis and PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf normalized', 'unit', 'mm', ...)`, comparing the returned cropped spatial support axis and normalized PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf xaxis', ..., 'airy disk', true)`, comparing the full returned support/data payload and Airy-disk radius from `s_wvfSpatial.m`; the headless Python path now also matches MATLAB by returning the full `uData` support for `psf xaxis` / `psf yaxis` instead of applying `plot range` cropping to the returned payload.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf xaxis', ...)`, comparing the full returned support/data payload from `s_wvfSpatial.m`.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf yaxis', ..., 'airy disk', true)`, comparing the full returned support/data payload and Airy-disk radius from `s_wvfSpatial.m`.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'psf yaxis', ...)`, comparing the full returned support/data payload from `s_wvfSpatial.m`.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., 'image psf angle', ...)`, comparing the returned angular support axis and PSF center row.
- That same script-driven WVF plotting surface now also has Octave-backed parity on the normalized `image psf angle` numerical contract, comparing the returned angular support axis and normalized PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d psf angle', ...)`, comparing the returned angular support axis and PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '2d psf angle normalized', ...)`, comparing the returned angular support axis and normalized PSF center row.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d psf', ...)`, comparing the returned cropped spatial support axis and raw PSF line slice.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d psf normalized', ...)`, comparing the returned cropped spatial support axis and normalized PSF line slice.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d psf angle', ...)`, comparing the returned cropped angular support axis and raw PSF line slice.
- That same script-driven WVF plotting surface now also has direct Octave parity on `wvfPlot(..., '1d psf angle normalized', ...)`, comparing the returned cropped angular support axis and normalized PSF line slice.
- That same optics helper surface now also includes `airy_disk` / `airyDisk` plus headless airy-disk overlay payloads for `wvfPlot(...)` and `oiPlot(...)`, with direct Octave parity on the scalar/image helper contract used by the upstream diffraction plotting workflows.
- That same script-driven WVF surface now also includes the spatial getter family used by `s_wvfSpatial.m`, including `calc nwave`, `psf sample spacing`, `ref psf sample interval`, `pupil sample spacing`, `pupil positions`, `pupil function amplitude`, and `pupil function phase`.
- That same script-driven WVF surface now also has direct Octave parity on the scalar control workflow from `s_wvfSpatial.m`, comparing how `npixels`, `pupil plane size`, and `focal length` change PSF sample spacing and `um per degree`.
- That same script-driven WVF surface now also includes `wvfOSAIndexToZernikeNM` / `wvfZernikeNMToOSAIndex`, with direct Octave parity on the OSA index round-trip used by `s_wvfWavefronts.m`.
- That same script-driven WVF helper surface now also includes headless `wvf_clear_data(...)` / `wvfClearData(...)`, `wvf_wave_to_idx(...)` / `wvfWave2idx(...)`, `wvf_osa_index_to_vector_index(...)` / `wvfOSAIndexToVectorIndex(...)`, and `wvf_osa_index_to_name(...)` / `wvfOSAIndexToName(...)` for the legacy MATLAB wavefront utility layer built on top of the existing WVF core.
- That same script-driven WVF helper surface now also includes headless `wvf_key_synonyms(...)` / `wvfKeySynonyms(...)`, `wvf_to_si_psf(...)` / `wvf2SiPsf(...)`, and `wvf_apply(...)` / `wvfApply(...)`, matching the remaining MATLAB key-canonicalization, shift-invariant PSF export, and deprecated scene-to-OI compatibility wrappers on top of the existing WVF compute path.
- That same script-driven WVF helper surface now also includes headless `psf_find_peak(...)` / `psfFindPeak(...)`, `psf_volume(...)` / `psfVolume(...)`, `psf_center(...)` / `psfCenter(...)`, and `psf_find_criterion_radius(...)` / `psfFindCriterionRadius(...)` for the legacy MATLAB PSF utility layer built on top of the existing WVF/PSF compute path.
- That same script-driven WVF helper surface now also includes headless `psf2lsf(...)`, `lsf2circularpsf(...)`, `psfCircularlyAverage(...)`, and `psfAverageMultiple(...)` for the legacy MATLAB PSF/LSF conversion and multi-PSF averaging helpers layered on top of the existing WVF/PSF compute path.
- That same script-driven WVF surface now also has direct Octave parity on the 16-coefficient OSA-index sweep from `s_wvfWavefronts.m`, comparing the returned support axis, normalized wavefront center rows and columns, and peak absolute wavefront amplitude at a 2 mm pupil.
- That same Phase 2 wavefront block now also has direct Octave parity on `s_zernikeInterpolation.m`, comparing the interpolated-vs-ground-truth Zernike coefficient vector at the target field height together with the normalized center-row PSFs for Zernike-space interpolation, ground truth, and the script's direct space-interpolation path.
- That same script-driven WVF plotting block now also has direct Octave parity on the end-to-end sequence in `s_wvfPlot.m`, comparing the 550 nm mixed-unit 1D PSF payloads and the recalculated 460 nm PSF-angle / pupil-phase payloads after `wvfSet(..., 'wave', 460)` and `wvfCompute(...)`.
- Sensor preset expansion now includes generic `rgbw` / `rccc` creation and upstream-backed `mt9v024` / `ar0132at` RGBW and RCCC model variants.
- `camera_create(...)` now forwards those expanded sensor presets too, so camera constructors can directly target `rgbw`, `rccc`, and vendor sensor variants like `mt9v024` / `ar0132at`.
- An initial optional `SessionContext` compatibility layer is now in place, with session-aware create/compute entry points, session-aware `camera_set(...)` / `ip_set(...)` replacement flows, MATLAB-style `vcReplaceObject` / `vcReplaceAndSelectObject` replacement helpers, object counting/listing/name helpers, MATLAB-style `vcGetSelectedObject` / `vcGetObject` pair-return semantics, delete helpers with slot renumbering plus `vcDeleteSomeObjects`, bulk object-list maintenance via `vcSetObjects`, slot-allocation helpers via `vcNewObjectValue`, add-and-select plus name-generation helpers via `vcAddAndSelectObject` / `vcNewObjectName`, nonpositive-selection clearing through `vcSetSelectedObject`, object-type reporting via `vcGetObjectType`, the matching `ieAddObject` / `ieDeleteObject` / `ieGetObject` / `ieGetSelectedObject` / `ieReplaceObject` / `ieSelectObject` wrappers including nested `optics` / `pixel` / `ipdisplay` getter semantics, a minimal `ieInitSession` / `ieSessionGet` / `ieSessionSet` layer for session metadata, preferences, GUI slots, selected-object queries, and image-size/GPU flags, plus window/figure aliases, `ieAppGet` for scene/OI/sensor/IP/display app lookup, the concrete GUI-side aliases still read upstream such as `scene image handle`, `sensorimagehandle`, `oiwindowHandles`, `vcimagehandles`, `metricshandles`, `oicomputelist`, and `sensor gamma`, session-backed `vcGetFigure` / `vcSelectFigure` / `ieMainClose` wrappers for stored window retrieval and cleanup, stored window geometry/state compatibility through `ieWindowsGet` / `ieWindowsSet`, the adjacent lightweight GUI/session wrappers `vcEquivalentObjtype`, `vcSetFigureHandles`, and `ieRefreshWindow`, headless placeholder creation for missing `vcSelectFigure(...)` / `ieRefreshWindow(...)` window state, name-based lookup compatibility through `ieEquivalentObjtype` / `ieFindObjectByName`, the shared MATLAB-style parameter parser `ieParameterOtype`, headless path-based `.mat` persistence through `vcSaveObject` / `vcExportObject` / `vcLoadObject`, a headless `iePTable` port for structured parameter-table output over the core object types, a headless `vcGetROIData` port for scene/OI/sensor/IP ROI extraction with MATLAB-style rect conversion, clipped bounds, XW-style row output, and NaN-filled sensor mosaic planes, the adjacent ROI conversion helpers `ieRect2Locs`, `vcRect2Locs`, `ieRoi2Locs`, `ieLocs2Rect`, and `ieRect2Vertices`, the first ROI-driven getter surface on top of that support through stored sensor ROI state, `sensorGet(..., 'roi volts/electrons/dv')`, `sensorGet(..., 'roi volts mean')`, `sensorGet(..., 'roi electrons mean')`, `ipGet(..., 'roidata'/'roixyz')`, and headless `imageDataXYZ`, plus the matching scene/OI ROI getter layer through `sceneGet(..., 'roi photons/energy/luminance/reflectance')`, `sceneGet(..., 'roi mean photons/energy/reflectance')`, and `oiGet(..., 'roi photons/energy')` with ROI-mean variants.
- Metrics expansion now includes initial `metricsSPD`-style spectral comparison plus generic MAE/RMSE/PSNR helpers, and now also the first nineteen script-driven Phase 4 metrics slices through `iso.py`, `camera.py`, `sensor.py`, the new `scielab.py`, the shared filter-asset loader, and the new display-linearization/filter-preparation/scene-add helpers: curated parity on `s_metricsEdge2MTF.m`, headless `ISO12233(...)` / `ieISO12233(...)` with curated slanted-edge parity on `s_metricsMTFSlantedBar.m`, the fixed-die-size pixel-sweep parity slice on `s_metricsMTFPixelSize.m`, the lux-sec pixel-SNR sweep parity slice on `s_metricsSNRPixelSizeLuxsec.m`, the infrared slanted-bar parity slice on `s_metricsMTFSlantedBarInfrared.m`, the camera-acutance parity slice on `s_metricsAcutance.m`, the Macbeth-camera color-accuracy parity slice on `s_metricsColorAccuracy.m`, the Macbeth Delta E parity slice on `s_metricsMacbethDeltaE.m`, the daylight spectral-metric sweep parity slice on `s_metricsSPD.m`, the camera VSNR parity slice on `s_metricsVSNR.m`, the LCD-Apple S-CIELAB tutorial parity slice on `t_metricsScielab.m`, the `crt.mat` S-CIELAB script parity slice on `s_rgb2scielab.m`, the `s_scielabExample.m` parity slice covering both the `scielabRGB(...)` branch and the explicit `dac2rgb(...)` / `imageLinearTransform(...)` walkthrough, the `s_scielabFilters.m` parity slice covering the point-spread, MTF, and filter-version branches of `scPrepareFilters(...)`, the `s_scielabMTF.m` parity slice covering the harmonic-sweep `sceneAdd(..., 'remove spatial mean')` workflow and the resulting CIELAB/S-CIELAB Delta E curves, the `s_scielabPatches.m` parity slice covering the uniform-patch illuminant-perturbation sweep and its quantized S-CIELAB summary contract, the `s_scielabMasking.m` parity slice covering the fixed-mask target-contrast sweep and its CIELAB / S-CIELAB masking curves, the `s_scielabTutorial.m` parity slice covering the multispectral Stuffed Animals rendered-RGB SCIELAB walkthrough and its opponent/filter/LAB summaries, and the `s_scielabHarmonicExperiments.m` parity slice covering the sweep-frequency gray-world/opponent-scaling SCIELAB experiment workflow.
- Initial ray-trace optics support is now in place: `oi_create('ray trace')` loads upstream ray-trace optics assets, and `oi_compute` applies geometric distortion, relative illumination, angle-aware precomputed PSFs with cached reuse from the pinned MATLAB data, MATLAB-style uncropped padding/output bookkeeping, and the core ray-trace PSF metadata `oiGet/oiSet` surface, including MATLAB-style whole-optics export via `oiGet('optics')`, MATLAB-style raw-struct export for `raytrace` / `rtpsf` / `rtgeometry` / `rtrelillum`, MATLAB-style `psfStruct` / `sampledRTpsf` export for the precomputed cache, MATLAB-style `psf struct` normalization, split semantics between precomputed `rtpsfsize` and raw `optics rtpsfsize`, unit-aware raw PSF spacing / field-height getters, derived support / frequency axes, raw geometry / relative-illumination table accessors, raw scalar ray-trace metadata, the legacy `rtcomputespacing` field, MATLAB-style `oiGet/oiSet('optics ...')` delegation, whole-struct `optics` / `raytrace` replacement that normalizes MATLAB-style payloads while invalidating stale cached PSFs, indexed raw-table updates for `rtpsfdata` and `rtgeomfunction`, direct `rt_psf_interp` / `rtPSFInterp` support, direct `rt_di_interp` / `rtDIInterp` plus `rt_ri_interp` / `rtRIInterp` helper ports that now also back the ray-trace geometry compute path, public helper ports for `rtSampleHeights`, `rtPSFGrid`, and `rtAngleLUT`, and staged wrapper ports for `rtGeometry`, `rtPrecomputePSF`, `rtPrecomputePSFApply`, and the legacy-compatible `rtPSFApply`.
- The MATLAB-style top-level ray-trace wrapper `optics_ray_trace` / `opticsRayTrace` is now implemented as the public staged entry point, with cached illuminance / mean-illuminance bookkeeping aligned to the computed OI state.
- The supporting OI helpers `oi_calculate_illuminance` / `oiCalculateIlluminance` and `oi_diffuser` / `oiDiffuser` are now public and back the `opticsRayTrace` blur/illuminance path.
- That same headless OI utility surface now also includes `oi_clear_data(...)` / `oiClearData(...)`, `oi_show_image(...)` / `oiShowImage(...)`, and `oi_save_image(...)` / `oiSaveImage(...)` for the legacy MATLAB computed-payload reset, direct RGB render, and PNG-export helpers.
- That same headless OI utility surface now also includes `oi_frequency_resolution(...)` / `oiFrequencyResolution(...)`, `oi_spatial_support(...)` / `oiSpatialSupport(...)`, and `oi_space(...)` / `oiSpace(...)` for the direct MATLAB OI geometry helper mini-family.
- That same headless OI utility surface now also includes `oi_psf(...)` / `oiPSF(...)` for the legacy MATLAB OI point-spread summary helper, covering thresholded PSF area and diameter measurements.
- That same headless OI utility surface now also includes `oi_calculate_irradiance(...)` / `oiCalculateIrradiance(...)`, `oi_adjust_illuminance(...)` / `oiAdjustIlluminance(...)`, `oi_interpolate_w(...)` / `oiInterpolateW(...)`, `oi_extract_waveband(...)` / `oiExtractWaveband(...)`, and `oi_add(...)` / `oiAdd(...)` for the legacy MATLAB OI spectral and irradiance helper mini-family.
- That same headless OI utility surface now also includes `oi_extract_bright(...)` / `oiExtractBright(...)` and `oi_from_file(...)` / `oiFromFile(...)`, matching the legacy MATLAB brightest-patch extraction helper and the scene-to-ray-trace OI wrapper on top of the existing `sceneFromFile(...)` / `sceneAdjustIlluminant(...)` path.
- That same headless OI utility surface now also includes `oi_illuminant_ss(...)` / `oiIlluminantSS(...)`, `oi_illuminant_pattern(...)` / `oiIlluminantPattern(...)`, and `oi_photon_noise(...)` / `oiPhotonNoise(...)`, together with direct `oiGet/oiSet(..., 'illuminant'/'illuminant photons')` support for the legacy MATLAB OI illuminant/noise helper mini-family.
- That same headless OI utility surface now also includes `oi_pad_value(...)` / `oiPadValue(...)`, `oi_pad(...)` / `oiPad(...)`, and `oi_make_even_row_col(...)` / `oiMakeEvenRowCol(...)` for the legacy MATLAB OI padding and even-dimension helper mini-family.
- That same headless OI utility surface now also includes `oi_pad_depth_map(...)` / `oiPadDepthMap(...)`, `oi_depth_segment_map(...)` / `oiDepthSegmentMap(...)`, `oi_depth_combine(...)` / `oiDepthCombine(...)`, and `oi_combine_depths(...)` / `oiCombineDepths(...)` for the legacy MATLAB OI depth-map padding, segmentation, and plane-combination helper mini-family.
- That same headless optics-object compatibility surface now also includes `optics_create(...)` / `opticsCreate(...)`, `optics_get(...)` / `opticsGet(...)`, `optics_set(...)` / `opticsSet(...)`, `optics_clear_data(...)` / `opticsClearData(...)`, `optics_description(...)` / `opticsDescription(...)`, `lens_list(...)` / `lensList(...)`, and `optics_to_wvf(...)` / `optics2wvf(...)` for the legacy MATLAB optics-object helper mini-family.
- That same headless optics helper surface now also includes `optics_defocus_depth(...)` / `opticsDefocusDepth(...)`, `optics_dl_compute(...)` / `opticsDLCompute(...)`, `optics_si_compute(...)` / `opticsSICompute(...)`, and `optics_plot_transmittance(...)` / `opticsPlotTransmittance(...)` for the legacy MATLAB optics depth/compute/transmittance wrapper family.
- That same focus/DoF optics block now also has direct Octave parity on `s_opticsDefocus.m`, comparing the blur/astigmatism/pupil-diameter WVF sequence through mean-photon conservation, recovered Zernike coefficients, pupil-diameter changes, and normalized 550 nm center-row irradiance slices.
- That same focus/DoF optics block now also includes public `optics_defocus_displacement` / `opticsDefocusDisplacement`, with direct Octave parity on `s_opticsDefocusDisplacement.m` for the base-power sweep, image-plane displacement curves, and the constant displacement-to-focal-length ratio check.
- That same focus/DoF optics block now also has direct Octave parity on `s_opticsDoF.m`, comparing the 100 mm F/2 thin-lens DOF formula result, the CoC-derived DOF crossing estimate, and the object-distance by f-number DOF sweep surface.
- That same focus/DoF optics block now also includes public `optics_depth_defocus` / `opticsDepthDefocus`, with direct Octave parity on `s_opticsDepthDefocus.m` for the default-lens object-distance sweep, the focal-plane and shifted-image-plane defocus curves, the shifted-plane in-focus object distance, and the pupil-scaled Hopkins `w20` surface.
- That same focus/DoF optics block now also includes public `optics_defocus_core` / `opticsDefocusCore` and `optics_build_2d_otf` / `opticsBuild2Dotf`, with direct Octave parity on `s_opticsDefocusScene.m` for the multispectral D65-adjusted defocus-image workflow, the derived 10 um and 40 um sensor-plane defocus values, and the normalized 550 nm center-row blur states for the focused and defocused OI outputs.
- That same focus/DoF optics block now also includes direct Octave parity on `s_opticsDefocusWVF.m`, comparing the explicit diffraction-limited and 1.5-diopter WVF branches with the alternate `oiCreate('wvf')` update path through the returned f-numbers, defocus coefficients, and normalized 550 nm PSF / OI center rows.
- The initial `rtOTF` helper groundwork is now public as well through `rtBlockCenter`, `rtExtractBlock`, `rtInsertBlock`, and `rtChooseBlockSize`.
- An initial public `rt_otf` / `rtOTF` block-wise ray-trace path is now exposed too, along with MATLAB-style `rtBlocksPerFieldHeight` control.
- The `rtOTF` filtered-block support helper is now public as `rt_filtered_block_support` / `rtFilteredBlockSupport`.
- The shared MATLAB field-height lookup rule is now public as `ie_field_height_to_index` / `ieFieldHeight2Index`, and `rtChooseBlockSize` now uses it directly.
- That same ray-trace helper surface now also includes `rt_root_path` / `rtRootPath` and the obsolete legacy helper `rt_image_rotate` / `rtImageRotate`; the audit now also classifies the upstream `rtBlockPartition.m` and `rtImagePSFFieldHeight.m` stubs as out of scope because they are diagnostic/nonfunctional rather than usable headless APIs.
- `rt_synthetic` / `rtSynthetic` is now exposed as a synthetic ray-trace optics generator to support controlled testing of the expanding ray-trace compute surface.
- That same ray-trace block now also has direct Octave parity on `s_opticsRTSynthetic.m`, comparing the synthetic geometry and relative-illumination curves, normalized 550 nm center/edge PSF slices, and the resulting point-array OI summary outputs from the increasing-Gaussian synthetic ray-trace workflow.
- That same ray-trace block now also has direct Octave parity on `s_opticsRTPSFView.m`, comparing the sampled-PSF view metadata, normalized 550 nm field-height and angle-sweep PSF rows, their stable blur-width summaries, and the canonicalized center/edge `rtPlot(..., 'psf', ...)` row profiles from the upstream PSF-view workflow.
- The initial Zemax text-import surface is now ported as `rt_file_names` / `rtFileNames`, `zemax_read_header` / `zemaxReadHeader`, `zemax_load` / `zemaxLoad`, and a limited `rt_import_data` / `rtImportData` path for standard ISETPARAMS + DI/RI/PSF bundles.
- `rt_import_data(...)` and `oi_create('ray trace', ...)` now accept that standard Zemax bundle directly as either an `ISETPARAMS.txt` / `ISETPARMS.TXT` file or a containing directory.
- The Zemax import path now also normalizes Windows-style `lensFile` / `baseLensFileName` strings when deriving DI/RI/PSF bundle filenames.
- The Zemax parameter-file parser now accepts multiline MATLAB vector syntax too, including `...` continuations, bracketed row/column wavelength lists, transpose-style vectors such as `[500 600]'`, and legacy single-line assignments without trailing semicolons like the older Zemax macros emit for `baseLensFileName`.
- `rt_import_data` now preserves existing optics-object name/transmittance/compute settings while updating the imported ray-trace top-level focal-length / f-number state to the effective values from the Zemax bundle, preserves a distinct existing ray-trace name independently from the top-level optics name, uses a raw `rayTrace.name` as the top-level optics-name fallback when no explicit top-level name is present, preserves raw MATLAB-style top-level control fields such as `computeMethod`, `offaxis`, and `aberrationScale`, preserves existing `blocks_per_field_height` settings, carries `psfSpacing` through to the normalized ray-trace computation metadata, preserves an existing computation spacing when a bundle omits that field, retains the requested `rtProgram` label on imported ray-trace optics, preserves real import errors when directory-based bundle loading hits a malformed Zemax bundle, enforces the upstream `psfSize` evenness check from the parameter file before loading PSF data, round-trips `blocksPerFieldHeight` through the raw MATLAB-style `oi_get('optics')` / `oi_set('optics', ...)` surface, treats normalized `blocks_per_field_height` plus scalar normalized fields like `f_number` and `magnification` as first-class normalized ray-trace keys during raw/normalized struct detection, merges both partial normalized nested `raytrace` updates and partial MATLAB-style nested `rayTrace` updates into the current ray-trace state instead of dropping existing geometry / PSF tables, lets both an explicit nested raw `rayTrace.fNumber` override the stale exported top-level `fNumber` and a top-level `focalLength` override the stale exported nested `rayTrace.effectiveFocalLength` during whole-optics updates, and now also honors normalized top-level aliases like `f_number`, `focal_length_m`, and `nominal_focal_length_m` plus raw exported fields like `nominalFocalLength` and `offaxis` in that same export-and-edit workflow. Whole-optics edits that change only `transmittance.wave` now also preserve the existing transmittance scale via interpolation instead of resetting it, while explicit whole-optics `transmittance.scale` edits now reject wrong-length vectors the same way the dedicated setter does.
- The headless ROI/plotting compatibility surface now also covers direct `sensorGet(..., 'electrons')` plus MATLAB-style line-profile queries through `sensorGet(..., 'hline volts/electrons/dv')` and `sensorGet(..., 'vline volts/electrons/dv')`, returning per-filter `data`, `pos`, and `pixPos` vectors.
- That same sensor plotting surface now also includes `sensorGet(..., 'chromaticity')` and `sensorGet(..., 'roi chromaticity mean')`, using the headless demosaic path to estimate per-pixel sensor chromaticity in vector or matrix form.
- The adjacent scene/OI/IP ROI plotting surface now includes matching chromaticity and summary getters too: `sceneGet(..., 'chromaticity'/'roi chromaticity mean')`, `sceneGet(..., 'roi mean luminance')`, `oiGet(..., 'chromaticity'/'roi chromaticity mean')`, `oiGet(..., 'roi illuminance'/'roi mean illuminance')`, and `ipGet(..., 'chromaticity'/'roi chromaticity mean')`.
- The next headless plotting layer is now covered too through explicit scene/OI line-profile getters: `sceneGet(..., 'radiance hline/vline')`, `sceneGet(..., 'luminance hline/vline')`, `sceneGet(..., 'spatial support linear')`, `oiGet(..., 'irradiance hline/vline')`, `oiGet(..., 'irradiance energy hline/vline')`, and `oiGet(..., 'illuminance hline/vline')`.
- The remaining adjacent scene illuminant getter surface is now covered too: `sceneGet(..., 'illuminant comment')`, `sceneGet(..., 'roi illuminant photons/energy')`, `sceneGet(..., 'roi mean illuminant photons/energy')`, and `sceneGet(..., 'illuminant hline/vline photons/energy')`.
- A minimal headless plotting wrapper layer now sits on top of those getters too: `plotScene`, `oiPlot`, `plotSensor`, and `ipPlot` all return MATLAB-style `uData` payloads and `None` instead of GUI figure handles for the currently supported ROI, chromaticity, illuminant, RGB-line, luminance-line, ROI histogram, ROI RGB-summary, ROI `CIELAB` / `CIELUV`, sensor line-profile, and sensor/pixel SNR plot types.
- The adjacent sensor characterization layer now also includes public `pixel_snr` / `pixelSNR` and `sensor_snr` / `sensorSNR` helpers, matching the MATLAB `pixelSNR` / `sensorSNR` curves over voltage swing and exposing shot/read/DSNU/PRNU limit curves where applicable.
- The current headless `plotSensor` subset now also includes spectral wrappers for `color filters` and `sensor spectral qe`, returning MATLAB-style `{x, y}` spectral payloads plus filter labels from the stored sensor spectral bundle, along with headless CFA views for `cfa` / `cfa block` and `cfa full` that follow MATLAB unit-block/full-array tiling and default image scaling.
- The adjacent headless sensor image-view surface is now covered too through `plotSensor('cfa image')`, `plotSensor('true size')`, and `plotSensor('channels')`, returning rendered image and per-channel payloads plus MATLAB-style sensor-name metadata instead of opening GUI windows, and the sensor render-control surface now includes explicit `gamma` / `scale max` state plus shared `sensorGet('rgb', ...)` rendering with `max output` / `max digital value` scaling semantics, with headless true-size / CFA-image payloads reporting the settings they use.
- The adjacent raw CFA compatibility surface now also includes `sensorGet/Set('cfa')`, `sensorGet('cfa pattern')`, `sensorGet('cfa name')`, and `sensorGet('unit block config')`, with MATLAB-style unit-block metadata derived from the stored pixel size and CFA block.
- The neighboring spectral metadata surface now also includes `sensorGet/Set('spectrum')`, wavelength aliases, `sensorGet('bin width')`, and MATLAB-style wave updates that keep the stored filter spectra, pixel QE, and IR-filter samples interpolated onto the new wavelength grid.
- The adjacent raw chart/metadata block is now covered too through `sensorGet/Set('chart parameters')`, `sensorGet/Set('chart corner points')`, `sensorGet/Set('chart rectangles')`, `sensorGet/Set('current rect')`, and the nearby `metadata sensor/scene/optics name` plus `metadata crop` round-trip fields, along with the neighboring movement metadata through `sensorGet/Set('sensor movement'/'eye movement')`, `sensorGet/Set('movement positions'/'sensor positions')`, and `sensorGet/Set('frames per position'/'exposure times per position')`. The neighboring legacy human-cone storage surface now round-trips too through `human`, `cone type`, `densities`, `xy`, and the `human cone seed` / `rseed` aliases. The neighboring legacy MCC aliases now round-trip too through `mcc corner points`, which reuses the chart corner-point field, plus raw `mcc rect handles` storage. The neighboring noise-summary aliases now also include `black level`, mapped onto the same stored zero-level state used by `zero level` / `zero`, the adjacent sensor data aliases now include `digital value` / `digital values`, routed through the existing `dv` storage path, `dv or volts` / `digital or volts` now follow the same preferred-DV fallback path as upstream, the adjacent headless sensor data-view surface now includes `volt images`, returning per-filter plane stacks with `NaN`-filled empty entries like MATLAB `plane2rgb(...)`, the same adjacent voltage/response block now also includes the upstream `voltage`, `electron`, and short-form `ag` / `ao` aliases for voltage, electron, analog-gain, and analog-offset access, the same `volts` / `dv` / `electrons` getters now support MATLAB-style channel-index extraction to per-filter vectors, the same adjacent sensor-response geometry surface now includes `pixel pd area` plus `electrons per area` with area-unit scaling and optional channel extraction, the neighboring quantization block now also includes `bits`, `qMethod`, `lut`, and `quantization structure` aliases while preserving the existing string-returning `quantization` getter used locally, the same adjacent raw color/filter block now also includes `color` plus the MATLAB cell-style aliases `filter names cell array`, `filter color names cell array`, and `filter names cell`, and the same sensor-facing passthrough now routes supported pixel electrical and geometric names such as `pixel name`, `pixel type`, `fill factor`, `pd area`, `conversion gain`, `conversion gain v per electron`, `volts per electron`, `well capacity`, `dark current`, `dark current per pixel`, `dark voltage per pixel`, `dark voltage per pixel per sec`, `volts per second`, `read noise`, `read noise volts`, `read standard deviation volts`, `read standard deviation electrons`, `pixel width`, `pixel width meters`, `pixel height`, `pixel height meters`, `width gap`, `height gap`, `width between pixels`, `height between pixels`, `pd width`, `pd height`, `pd size`, `pd dimension`, `pixel area`, `pixel depth meters`, `pixel layer thicknesses`, `refractive index`, `pixel refractive indices`, `n`, `pixel spectrum` / `pixel wavelength`, `pixel bin width` / `pixel nwave`, `pixel dr`, `pd xpos`, `pd ypos`, `pd position`, `photodetector x position`, `photodetector y position`, `pixel quantum efficiency`, `photodetector quantum efficiency`, and `photodetector spectral quantum efficiency` through the stored pixel model, including whole-`pixel` replacement via `sensorSet('pixel', ...)`. The direct unprefixed sensor-routing path now also covers the remaining unique pixel setters such as `width and height`, `pd width and height`, `size same fill factor`, `size constant fill factor`, `size keep fill factor`, and `dark voltage per pixel per sec` without requiring an explicit `pixel` prefix. The same adjacent unit/electrical alias surface now also includes the meter-suffixed pixel getters plus the direct `conversion gain v per electron` form, and the neighboring material-alias surface now supports direct `refractive index` and `n` access without requiring the explicit `pixel` prefix. That same stored photodetector-position metadata now also feeds the internal photodetector array placement used by sensor computation, the adjacent `read noise` setter semantics now match MATLAB by interpreting raw `read noise` values in electrons rather than volts, the adjacent MATLAB pair-setter aliases `pixel width and height` and `pixel pd width and height` now accept `[width, height]` inputs without changing the existing local `pixel size` / `pd size` semantics, and the adjacent `pixel size same fill factor` / `pixel size constant fill factor` setter semantics now match MATLAB by scaling the photodetector geometry with the pixel size instead of behaving like a plain pixel-size replacement. The neighboring sensor model surface now includes raw `diffusion MTF` storage for legacy compatibility. The legacy `scene_name` / `lens` aliases now round-trip with that same sensor metadata surface as well, including `metadata lensname` / `metadata lens` getters, the adjacent raw `microlens` / `ml` plus `microlens offset` / `mloffset` storage surface now round-trips too, the remaining scalar hooks in that same upstream block now round-trip through `consistency` / `sensor consistency` plus `sensor compute` / `sensor compute method`, the neighboring exposure-control surface now includes `n exposures`, `exposure plane`, `cds`, and MATLAB-style `auto exposure` string handling, the adjacent exposure-summary surface now includes `exposure method`, array-capable `integration time` / `exptimes`, `unique exptimes`, and `central exposure` with explicit `sensorCompute` rejection for unsupported multi-exposure arrays, the nearby sampling/vignetting aliases now round-trip through `pixel samples` / `n pixel samples for computing` plus `sensor vignetting` / `sensor bare etendue` / `no microlens etendue`, the adjacent noise-control storage surface now includes `reuse noise`, `noise seed`, and `response type` with stored noise seeds feeding `sensorCompute(...)` when no explicit seed is passed, the neighboring column fixed-pattern-noise storage surface now round-trips too through `column fpn`, `column dsnu` / `column prnu`, and the raw column offset/gain FPN vectors, and the same adjacent response/noise summary block now includes `response dr`, `dr` / `dynamic range` / `sensor dynamic range`, plus the `shot noise flag` getter alias.
- The neighboring normalized pixel electrical shorthand surface now also supports direct `conversiongainvpelectron`, `vswing`, `darkvolt`, and `darkvolts` access without requiring the explicit `pixel` prefix.
- The adjacent QE alias surface now also supports direct `quantum efficiency` access without requiring the explicit `pixel` prefix.
- The neighboring FPN setter surface now also supports the upstream `offset noise value` and `gain noise value` aliases.
- The same FPN block now also has parser-level support for the broader MATLAB alias family around DSNU/PRNU and column-FPN names such as `sigma offset fpn`, `gain fpn`, `column fixed pattern noise`, and `col gain`.
- That same FPN parser surface now also recognizes the setter-only `column fpn parameters` alias.
- The neighboring sensor metadata block now also has parser-level support for `scene_name`, `metadata scene name`, `lens`, `metadata lensname`, `metadata optics name`, `metadata sensor name`, and `metadata crop`.
- The adjacent sensor-data compatibility surface now also has exact runtime/parser coverage for the no-space aliases `digitalvalue`, `digitalvalues`, `dvorvolts`, `digitalorvolts`, `voltimages`, `voltage`, `electron`, `ag`, and `ao`.
- The neighboring quantization and color/filter compatibility surface now also has exact runtime/parser coverage for the no-space aliases `qmethod`, `quantizationstructure`, `quantizationlut`, `filternamescellarray`, `filtercolornamescellarray`, and `filternamescell`.
- The adjacent CFA and spectral-metadata compatibility surface now also has exact runtime/parser coverage for the no-space aliases `cfapattern`, `cfaname`, `unitblockconfig`, `sensorspectrum`, `wavelengthresolution`, `binwidth`, and `numberofwavelengthsamples`.
- The neighboring filter/response metadata compatibility surface now also has exact runtime/parser coverage for the no-space aliases `nfilters`, `filtercolorletters`, `filtercolorletterscell`, `filterplotcolors`, `patterncolors`, `spectralqe`, and `sensorspectralsr`.
- The adjacent spectral-filter and pixel-response compatibility surface now also has exact runtime/parser coverage for the no-space aliases `filterspectra`, `colorfilters`, `infraredfilter`, `irfilter`, `pixelspectralqe`, `pdspectralqe`, `pixelqe`, `spectralsr`, `pdspectralsr`, `pixelspectralsr`, and `sr`.
- The neighboring geometry/block metadata compatibility surface now also has exact runtime/parser coverage for the no-space aliases `unitblockrows`, `unitblockcols`, and `cfasize`, while the runtime surface now also uses the exact sensor getter form `spatialsupport`.
- The neighboring direct sensor geometry/data surface now also has exact runtime coverage for `rows`, `cols`, `size`, `dimension`, `arraywidth`, `arrayheight`, `wspatialresolution`, `hspatialresolution`, `deltax`, and `deltay`, with parser coverage intentionally staying on the prefixed forms like `sensor rows` because the bare names remain object-generic in `ie_parameter_otype(...)`.
- The adjacent direct sensor data-access surface now also has exact runtime coverage for `volts`, `voltage`, `dv`, `digitalvalue`, `digitalvalues`, `electrons`, `electron`, `dvorvolts`, `digitalorvolts`, `voltimages`, and channel-select access through the exact no-space aliases, while parser coverage for the ambiguous bare names stays on the prefixed forms like `sensor volts`.
- The adjacent ROI/response surface now also has exact runtime/parser coverage for `roivolts`, `roielectrons`, `roidv`, `roivoltsmean`, `roielectronsmean`, `responseratio`, and `volts2maxratio`.
- That same ROI/response surface now also has exact runtime/parser coverage for the ROI-data synonym family `roidata`, `roidatav`, `roidatavolts`, `roidatae`, `roidataelectrons`, and `roidigitalcount`, plus the exact line-profile aliases `hlinevolts`, `hlineelectrons`, `hlinedv`, `vlinevolts`, `vlineelectrons`, and `vlinedv`.
- That same sensor ROI and line-profile surface now also exercises the exact getter form `roichromaticitymean`, with prefixed parser coverage for `sensor chromaticity` and `sensor roichromaticitymean`.
- The adjacent ROI geometry surface now also has exact runtime/parser coverage for `roi`, `roilocs`, and `roirect`.
- The neighboring microlens/vignetting/etendue surface now also has exact runtime/parser coverage for `microlens`, `microlensoffset`, `microlensoffsetmicrons`, `vignetting`, `ngridsamples`, and `sensoretendue`.
- The adjacent quantization surface now also has exact runtime/parser coverage for `quantization`, `quantizationmethod`, `nbits`, `quantizatonlut`, `maxdigital`, and `maxoutput`.
- The neighboring CFA/filter metadata surface now also has exact runtime/parser coverage for `pattern`, `filternames`, and `diffusionmtf`.
- The adjacent FPN/noise-control surface now also has exact runtime/parser coverage for `dsnusigma`, `offsetnoisevalue`, `prnusigma`, `gainnoisevalue`, `fpnparameters`, `columnfixedpatternnoise`, `coloffsetfpnvector`, `colgainfpnvector`, and `noiseflag`.
- The same neighboring FPN-image surface now also has exact runtime/parser coverage for `dsnuimage`, `offsetfpnimage`, `prnuimage`, and `gainfpnimage`, including clearing those stored images with `None`.
- The neighboring exposure-summary surface now also has exact runtime/parser coverage for `exptime`, `exposuretimes`, `exposuretime`, `expduration`, `exposureduration`, `exposuredurations`, `uniqueintegrationtimes`, `uniqueexptime`, `uniqueexptimes`, `geometricmeanexposuretime`, `expmethod`, and `cds`.
- The adjacent movement and legacy-human surface now also has exact runtime/parser coverage for `sensormovement`, `framesperpositions`, `conetype`, `conexy`, and `conelocs`.
- The adjacent control/summary compatibility surface now also has exact runtime/parser coverage for the no-space aliases `autoexposure`, `integrationtime`, `integrationtimes`, `nsamplesperpixel`, `sensorconsistency`, `sensorcompute`, `sensorcomputemethod`, `dynamicrange`, `sensordynamicrange`, `maxdigitalvalue`, `analoggain`, and `analogoffset`.
- The neighboring pixel passthrough compatibility surface now also has exact runtime/parser coverage for the no-space aliases `fillfactor`, `pixelsize`, `pixelwidth`, `pixelheight`, `pixelwidthmeters`, `pixelheightmeters`, `widthgap`, `heightgap`, `widthbetweenpixels`, `heightbetweenpixels`, `xyspacing`, `pdarea`, `voltsperelectron`, `wellcapacity`, `maxvoltage`, `darkvoltageperpixel`, `darkelectrons`, `darkcurrentdensity`, `readnoisevolts`, `readnoiseelectrons`, `readstandarddeviationvolts`, `readnoisemillivolts`, `pixelspectrum`, `pixelwavelength`, `pixelbinwidth`, `pixelnwave`, `layerthicknesses`, `refractiveindices`, `stackheight`, `pixeldepthmeters`, `pdposition`, `pdxpos`, `pdypos`, `photodetectorwidth`, `photodetectorheight`, `photodetectorsize`, `photodetectorxposition`, `photodetectoryposition`, `pdwidthandheight`, `pixeldynamicrange`, `darkvoltageperpixelpersec`, `voltspersecond`, `readnoisestdvolts`, `readstandarddeviationelectrons`, `widthandheight`, `widthheight`, `sizeconstantfillfactor`, `sizekeepfillfactor`, `sizesamefillfactor`, and `pddimension`.
- The adjacent microlens block now also has parser-level support for `ml`, `mlens`, `ulens`, `microlens offset`, `mloffset`, and `microlens offset microns`.
- The adjacent consistency block now also has parser-level routing for `consistency`, `sensor consistency`, and `sensor compute`.
- The neighboring exposure block now also has parser-level routing for `integration time`, `exptimes`, `unique exptimes`, `central exposure`, `exposure method`, `n exposures`, `exposure plane`, `correlated double sampling`, `auto exposure`, and `automatic exposure`.
- That same exposure coverage now also exercises the exact no-space aliases `integrationtime`, `uniqueexptimes`, `centralexposure`, `exposuremethod`, `nexposures`, `exposureplane`, `correlateddoublesampling`, `autoexp`, `autoexposure`, and `automaticexposure`.
- The neighboring sampling/noise-control block now also has parser-level routing for `pixel samples`, `n pixel samples for computing`, `spatial samples per pixel`, and `response type`.
- That same sampling/noise-control coverage now also exercises the exact no-space aliases `pixelsamples`, `npixelsamplesforcomputing`, `spatialsamplesperpixel`, `reusenoise`, `noiseseed`, and `responsetype`.
- The adjacent movement block now also has parser-level routing for `eye movement`, `frames per position`, `exposure times per position`, and `etime per pos`.
- The same movement alias surface now also has focused parser/runtime coverage for `movement positions` plus the prefixed `sensor positions` and `sensor positions x/y` forms.
- That same movement coverage now also exercises the exact no-space aliases `eyemovement`, `movementpositions`, `sensorpositions`, `sensorpositionsx`, `sensorpositionsy`, `framesperposition`, `exposuretimesperposition`, and `etimeperpos`.
- The neighboring legacy human block now also has parser-level routing for `human`, `cone type`, `densities`, `xy`, and `rseed`.
- That same legacy human block now also routes the location aliases `cone xy` and `cone locs`.
- That same legacy human surface now also has focused parser/runtime coverage for the long-form aliases `human cone type` and `human cone locs`.
- The same legacy human runtime coverage now also exercises `human cone densities` and `human cone seed` directly.
- That same legacy human runtime/parser coverage now also exercises the exact legacy alias `humanrseed`, and the same runtime coverage now also exercises the exact location aliases `conexy` and `conelocs`.
- The same legacy human coverage now also exercises the exact no-space aliases `humanconetype`, `humanconedensities`, and `humanconelocs`, with parser coverage also including `humanconeseed`.
- The adjacent chart block now also has parser-level routing for `chart parameters`, `corner points`, `chart corners`, `chart rects`, `chart rectangles`, `current rect`, and `chart current rect`.
- That same chart surface now also has focused parser/runtime coverage for the long-form setter alias `chart corner points`.
- That same chart runtime coverage now also exercises the long-form setter path for `chart rectangles` and `current rect`, plus the adjacent `mcc corner points` setter alias.
- That same chart coverage now also exercises the exact no-space aliases `chartparameters`, `cornerpoints`, `chartcorners`, `chartrects`, `currentrect`, and `mccrecthandles`.
- The same runtime coverage now also exercises the exact long no-space chart aliases `chartcornerpoints`, `chartrectangles`, and `chartcurrentrect`.
- The neighboring metadata block now also has focused parser/runtime coverage for `scene name`, `metadata scene name`, `metadata lens name`, `metadata lens`, `metadata optics name`, `metadata sensor name`, and `metadata crop`.
- That same metadata runtime coverage now also exercises the exact aliases `scene_name`, `scenename`, `metadatalensname`, `metadatalens`, `metadatasensorname`, and `metadatacrop`.
- The adjacent chart/metadata coverage now also exercises the exact no-space MCC and metadata aliases `mcccornerpoints`, `mccrecthandles`, `metadatascenename`, `metadataopticsname`, `metadatasensorname`, and `metadatacrop`.
- The neighboring vignetting alias block now also has parser-level support for `vignetting flag`, `pixel vignetting`, `sensor vignetting`, `sensor bare etendue`, `no microlens etendue`, and `vignetting name`.
- That same vignetting coverage now also exercises the exact no-space aliases `vignettingflag`, `pixelvignetting`, `sensorvignetting`, `sensorbareetendue`, `nomicrolensetendue`, and `vignettingname`.
- That same noise-summary coverage now also exercises the exact no-space aliases `columnfpnparameters`, `columnfpn`, `columnfpnoffset`, `columnfpngain`, `coloffsetfpn`, `coloffset`, `colgainfpn`, `colgain`, `responsedr`, `drdb20`, `shotnoiseflag`, `blacklevel`, and `zerolevel`.
- The same headless sensor plotting surface now also covers `plotSensor('etendue')`, returning MATLAB-style support and relative-illumination payloads without opening a mesh plot window.
- The sensor spectral-wrapper surface now also includes `plotSensor('ir filter')`, `plotSensor('pixel spectral qe')`, `plotSensor('pixel spectral sr')`, and `plotSensor('sensor spectral sr')`, backed by explicit `ir filter` and `pixel spectral qe` state plus MATLAB-style combined `sensorGet(..., 'spectral qe')` / `sensorGet(..., 'sensor spectral sr')` semantics.
- That same headless sensor plotting layer now also covers deterministic noise-map payloads for `plotSensor('shot noise')`, `plotSensor('dsnu')`, and `plotSensor('prnu')`, backed by MATLAB-style DSNU/PRNU aliases on the sensor object.
- A matching headless `plotSensorFFT(...)` wrapper is now in place too for monochrome horizontal/vertical line FFT payloads, carrying MATLAB-style `cpd`, `amp`, `mean`, and `peakContrast` data without opening a graph window.
- The adjacent headless sensor chromaticity plot is now covered too through `plotSensor('chromaticity')`, returning MATLAB-style `rg`, `rect`, and `spectrumlocus` payloads for ROI-driven sensor chromaticity plots.
- The supported headless sensor line-profile plots now also accept MATLAB-style adjacent-line aggregation through `plotSensor(..., 'two lines', True)`, returning merged `pixPos`, `pixData`, and `pixColor` payloads on top of the existing line-profile surface.
- That same headless `plotSensor(...)` wrapper now also accepts MATLAB-style `capture` selection for multi-capture sensor stacks, with `sensorGet(..., 'n captures')` reporting the available capture count from stacked volts/DV data.
- The adjacent line/FFT metadata surface is tighter now too: single-line sensor plots return plot labels plus filter-color metadata, and `plotSensorFFT(...)` accepts the same `capture` selection path for multi-capture stacks while exposing the MATLAB y-axis label.
- The remaining supported histogram/chromaticity wrappers now return the corresponding headless plot metadata too: histogram axis labels plus filter-color metadata, and MATLAB-style `r/g` chromaticity axis labels with the `rg sensor chromaticity` title string.
- The supported spectral and etendue wrappers now expose the same core plot metadata as well: spectral wavelength-axis labels plus MATLAB-style name strings, and etendue x/y/z axis labels with the matching `ISET: Etendue (...)` name.
- The headless `plotSensor('pixel snr')` / `plotSensor('sensor snr')` wrappers now return the matching plot metadata too: `Signal (V)` / `SNR (db)` axis labels, MATLAB-style title strings, and legend entries for the component SNR curves.

## Summary
- Build a new Python library, `pyisetcam`, that ports the numerical `scene -> optical image -> sensor -> image processor -> camera` pipeline from ISETCam and validates it against GNU Octave runs of the upstream MATLAB code.
- Pin the migration baseline to upstream commit `412b9f9bdb3262f2552b96f0e769b5ad6cdff821` so the port is reproducible and not tied to a moving `main` branch.
- Treat milestone one as a hard-scope core port. The upstream repo is large, asset-heavy, and mixed with GUI/App Designer, global session state, MEX/OpenEXR, and facetracker code that should not block the first Python deliverable.
- Grounding sources: [ISETCam repo](https://github.com/ISET/isetcam), [pinned commit](https://github.com/ISET/isetcam/tree/412b9f9bdb3262f2552b96f0e769b5ad6cdff821), [README](https://raw.githubusercontent.com/ISET/isetcam/412b9f9bdb3262f2552b96f0e769b5ad6cdff821/README.md), [LICENSE](https://raw.githubusercontent.com/ISET/isetcam/412b9f9bdb3262f2552b96f0e769b5ad6cdff821/LICENSE), [Miniforge](https://github.com/conda-forge/miniforge), [conda-forge Octave](https://anaconda.org/conda-forge/octave).
- Local constraints to plan around: the workspace is empty and not a git repo, macOS Command Line Tools are missing, `/usr/bin/python3` and `/usr/bin/git` are installer shims, and `octave` is not installed.

## Scope
- In scope for milestone one: `sceneCreate`, `sceneAdjustIlluminant`, `sceneGet/Set`, `displayCreate`, `oiCreate`, `oiCompute`, `oiGet/Set`, `sensorCreate`, `sensorCompute`, `sensorGet/Set`, `ipCreate`, `ipCompute`, `ipGet/Set`, `cameraCreate`, `cameraCompute`, `cameraGet/Set`, plus shared color, spectral, units, and asset-loading helpers.
- Supported option set in milestone one: default Macbeth scenes plus `uniform d65`, `uniform ee`, `checkerboard`, `slanted bar`; diffraction-limited and wavefront-based shift-invariant optics; sensor types `default/bayer-grbg`, `bayer-rggb`, `monochrome`, and `ideal`; default IP pipeline with bilinear, nearest-neighbor, Laplacian, and adaptive-Laplacian Bayer demosaic plus display rendering; default, monochrome, and ideal camera constructors.
- Explicitly out of scope in milestone one: `vcSESSION` globals and implicit object lookup, GUIs/`.mlapp`/`.fig`, ray-trace optics, OpenEXR MEX, facetracker, metrics, printing, peripheral human/display tools, and most vendor-specific sensor presets.
- Any call outside that support matrix must raise `NotImplementedError` with the original MATLAB function name and requested option in the error text.

## Repository And Toolchain
- Initialize the empty workspace as a new git repo with a `src/` layout.
- Install Miniforge first, then create one environment named `isetcam-py` containing `python=3.12`, `octave`, `numpy`, `scipy`, `scikit-image`, `matplotlib`, `imageio`, `h5py`, `pooch`, `pytest`, `pytest-cov`, `ruff`, and `mypy`.
- Do not rely on system `python3`, system `git`, or a system Octave install; the current machine state is not usable for this project.
- Add `tools/fetch_upstream.py` that downloads the pinned ISETCam tarball into `.cache/upstream/isetcam/<sha>/` and verifies SHA256. Do not commit the full upstream MATLAB repo into the new Python repo.
- Add `tools/octave/run_case.m` and `tools/octave/export_case.m` to execute reference cases in Octave and save canonical outputs with `save('-mat7-binary', ...)`.

## Important Changes Or Additions To Public APIs / Interfaces / Types
- Package import path: `pyisetcam`.
- Canonical Python API uses snake_case: `scene_create`, `oi_compute`, `sensor_compute`, `ip_compute`, `camera_compute`. Re-export thin MATLAB-style aliases: `sceneCreate = scene_create`, `oiCompute = oi_compute`, and so on.
- Core types are flexible dataclasses, not raw dicts: `Scene`, `OpticalImage`, `Sensor`, `ImageProcessor`, `Display`, and `Camera`. Each has `name`, `type`, `metadata`, and a mutable `fields`/`data` store so MATLAB-style extensibility is preserved.
- Add shared `param_format()` that mirrors `ieParamFormat`: lowercase keys and strip spaces from keys only.
- All compute APIs require explicit object arguments. Any MATLAB behavior that would default to `vcSESSION` is unsupported in milestone one.
- Internal array convention is `(rows, cols, wave)` with wavelengths in nanometers and spatial units stored in meters. Use `float64` throughout milestone one to minimize parity drift.
- Asset resolution goes through an `AssetStore` abstraction that loads upstream `.mat`, `.json`, `.dat`, and image assets from `.cache/upstream/...` and never hardcodes user-specific paths.

## Implementation Phases
1. Bootstrap the repo, packaging, lint/test config, Miniforge environment instructions, and upstream asset fetcher. Exit gate: clean environment creation and successful download of the pinned upstream snapshot.
2. Port shared foundations: dataclasses, `param_format`, unit conversion helpers, spectral interpolation, `.mat`/JSON/image loaders, and error classes. Exit gate: deterministic unit tests pass without Octave.
3. Port scene, display, and color basics: `scene_create`, `scene_get/set`, `scene_adjust_illuminant`, luminance helpers, `display_create`, and the spectral/color transforms needed by the selected parity cases. Exit gate: parity on Macbeth/default, uniform scenes, and illuminant adjustment.
4. Port optics and optical image: `oi_create`, `oi_get/set`, diffraction-limited OTF/PSF path, shift-invariant wavefront path, padding, cropping, and resampling. Defer ray-trace optics. Exit gate: parity on default scene -> OI and one small wavefront case.
5. Port the sensor stack: essential pixel helpers, supported CFA patterns, `sensor_set_size_to_fov`, exposure handling, `sensor_compute`, noise flag `0` and `2`, analog gain/offset, clipping, and quantization. Exit gate: parity on noiseless volts and seeded statistical checks for noisy output.
6. Port image processing and camera orchestration: `ip_create`, `ip_compute`, bilinear plus MATLAB-style nearest-neighbor/Laplacian/adaptive-Laplacian Bayer demosaic, default adaptive transform path, display rendering, and `camera_create/compute/get/set`. Exit gate: end-to-end camera parity on curated cases.
7. Add docs and examples: one migration guide mapping MATLAB names to Python names, one end-to-end notebook or script, a machine-readable parity report in `reports/parity/`, and a machine-readable migration gap ledger in `docs/migration-gap-ledger.yaml` with the companion audit summary in `reports/migration-gap/`. Exit gate: a new engineer can run the curated cases and inspect the remaining migration surface without opening MATLAB.
8. Keep the migration-gap audit honest: explicitly classify already-landed low-level helpers, deprecated aliases, MATLAB camelcase-to-Python snake_case wrapper matches, short prefixed MATLAB wrapper names such as `oiGet` / `oiSet` / `oiAdd`, and GUI-only hooks so the remaining backlog reflects real compute gaps rather than false-positive family debt.

## Test Cases And Scenarios
- `scene_macbeth_default`: `sceneCreate()` equivalent, compare wave axis, photons shape, mean luminance, and selected pixel spectra.
- `scene_checkerboard_small`: `sceneCreate('checkerboard', 8, 4)` parity, compare wave axis, photons cube, and mean luminance for a non-Macbeth synthetic scene.
- `scene_uniform_bb_small`: `sceneCreate('uniform bb', 16, 4500)` parity, compare wave axis, photons cube, and mean luminance for direct blackbody scene construction.
- `scene_frequency_orientation_small`: `sceneCreate('frequency orientation', params)` parity on the upstream demosaic target, compare wave axis, photons cube, and mean luminance for a script-driven scene family.
- `scene_harmonic_small`: `sceneCreate('harmonic', params)` parity on a two-component windowed harmonic pattern, compare wave axis, photons cube, and mean luminance for a script-driven scene family.
- `scene_sweep_frequency_small`: `sceneCreate('sweep frequency', 64, 12)` parity, compare wave axis, photons cube, and mean luminance for the script-driven spatial-frequency sweep target.
- `scene_reflectance_chart_small`: `sceneCreate('reflectance chart', 8, sSamples, sFiles, [], true, 'without replacement')` parity on a deterministic explicit-sample reflectance chart, compare wave axis, photons cube, mean luminance, and chart layout bookkeeping.
- `scene_star_pattern_small`: `sceneCreate('star pattern', 64, 'ee', 6)` parity on the upstream Siemens-star scene family, compare wave axis, photons cube, and mean luminance for a script-driven radial-line target.
- `utility_unit_frequency_list`: `unitFrequencyList(50/51)` parity, compare even- and odd-length normalized frequency support for a standalone helper function.
- `utility_energy_quanta_1d`: `Energy2Quanta` / `Quanta2Energy` parity on a 1D spectrum, compare wave, photons, and round-trip-restored energy for a standalone conversion helper.
- `utility_energy_quanta_matrix`: `Energy2Quanta` / `Quanta2Energy` parity on a wave-first energy matrix, compare wave, photons matrix, and round-trip-restored energy for the matrix conversion path.
- `utility_blackbody_energy_small`: `blackbody(..., 'energy')` parity on two fixed color temperatures, compare wave, temperatures, and the returned spectral energy matrix for a standalone illumination helper.
- `utility_blackbody_quanta_small`: `blackbody(..., 'photons')` parity on two fixed color temperatures, compare wave, temperatures, and the returned photon matrix for a standalone illumination helper.
- `utility_ie_param_format_string`: `ieParamFormat('Exposure Time')` parity on a deterministic string input, compare the original and formatted string result for the core parameter-normalization helper.
- `metrics_xyz_from_energy_1d`: `ieXYZFromEnergy` parity on a 1D SPD, compare wave, energy, and XYZ tristimulus output for the first metrics-family helper case.
- `metrics_xyz_to_luv_1d`: `xyz2luv` parity on a single XYZ/white-point pair, compare the XYZ input, white point, and resulting LUV vector for another deterministic metrics-family helper.
- `metrics_xyz_to_lab_1d`: `ieXYZ2LAB` parity on a single XYZ/white-point pair, compare the XYZ input, white point, and resulting LAB vector for another deterministic metrics-family helper.
- `metrics_xyz_to_uv_1d`: `xyz2uv(..., 'uv')` parity on a single XYZ vector, compare the XYZ input and resulting CIE 1960 uv chromaticity pair for another deterministic metrics-family helper.
- `metrics_cct_from_uv_1d`: `cct(uv)` parity on one fixed CIE 1960 uv pair, compare the uv input and resulting correlated color temperature from the upstream `cct.mat` lookup table.
- `metrics_delta_e_ab_1976_1d`: `deltaEab(..., '1976')` parity on one XYZ pair and white point, compare the XYZ inputs, white point, and resulting 1976 Delta E scalar for a deterministic metrics-family helper.
- `metrics_spd_angle_1d`: `metricsSPD(..., 'angle')` parity on two short orthogonal SPDs, compare wave, both SPDs, and the resulting angle scalar for a deterministic metrics-family helper.
- `metrics_spd_cielab_1d`: `metricsSPD(..., 'cielab')` parity on two fixed SPDs, compare wave, both SPDs, resulting delta E scalar, XYZs, LABs, and derived white point for another deterministic metrics-family helper.
- `metrics_spd_mired_1d`: `metricsSPD(..., 'mired')` parity on two fixed blackbody SPDs, compare wave, both SPDs, the resulting mired scalar, the upstream uv coordinates, and the correlated color temperatures derived from the `cct.mat` lookup table.
- `scene_illuminant_change`: apply a 3000 K blackbody illuminant with `preserveMean=True` and `False`; compare luminance preservation and spectral ratios.
- `scene_cct_blackbody_small`: `s_sceneCCT.m` parity on the blackbody correlated-color-temperature workflow, compare the 3500 K reference SPD, the 3500/6500/8500 K single-spectrum CCT estimates, and the 4500:1000:8500 K multi-spectrum blackbody/CCT sweep.
- `scene_daylight_small`: `s_sceneDaylight.m` parity on the daylight-illuminant workflow, compare the photon and energy daylight spectra, their luminance vectors, the per-spectrum 100 cd/m^2 normalized outputs, the CIE daylight basis, and the three basis-weight examples.
- `scene_illuminant_small`: `s_sceneIlluminant.m` parity on the illuminant-object workflow, compare the default and 3000 K blackbody illuminants, D65 200 cd/m^2, equal-energy and equal-photons spectra, illuminant C, 555 nm monochromatic construction, D65 wave-resampling, and fluorescent/tungsten spectra.
- `scene_illuminant_mixtures_small`: `s_sceneIlluminantMixtures.m` parity on the spatial-spectral mixed-illuminant workflow, compare the mixed-scene size, split row, top/bottom mean illuminant spectra, preserved top/bottom reflectance spectra, and mean luminance.
- `scene_illuminant_space_small`: `s_sceneIlluminantSpace.m` parity on the spatial-spectral illuminant workflow, compare the converted scene size/shape, row-temperature illuminant summaries, reflectance preservation after row and column modulation, the target column-harmonic profile at 550 nm, and the final row/column branch profile.
- `scene_xyz_illuminant_transforms_small`: `s_sceneXYZilluminantTransforms.m` parity on the D65-versus-tungsten XYZ transfer workflow, compare the scene size, normalized mean XYZ balance, the fitted full and diagonal 3x3 transforms, and their per-channel relative prediction RMSEs.
- `scene_from_rgb_lcd_apple_small`: `s_sceneFromRGB.m` parity on the display-calibrated RGB-image workflow, compare the LCD-Apple display wave/SPD and white chromaticity, the resulting scene size/luminance and normalized mean photons, the adjusted 6500 K illuminant spectrum, and the selected ROI mean reflectance.
- `scene_from_multispectral_stuffed_animals_small`: `s_sceneFromMultispectral.m` parity on the StuffedAnimals multispectral-file workflow, compare the loaded scene size, wave support, mean luminance, and normalized mean/center scene spectra on the `400:10:700` grid.
- `scene_from_rgb_vs_multispectral_stuffed_animals_small`: `s_sceneFromRGBvsMultispectral.m` parity on the StuffedAnimals multispectral roundtrip workflow, compare the source and reconstructed scene sizes, source and reconstructed wave grids, preserved mean luminance, preserved 6500 K illuminant chromaticity, and the per-channel RGB/XYZ roundtrip correlations.
- `scene_data_extraction_plotting_small`: `s_sceneDataExtractionAndPlotting.m` parity on the default `macbethd65` scene data-extraction workflow, compare the luminance-hline spatial support and normalized profile, illuminant-energy spectrum/comment, the yellow-patch ROI radiance/reflectance summaries, ROI shape, and exact agreement between `scenePlot(...)` ROI spectra and manual `vcGetROIData(...)` means.
- `scene_monochrome_small`: `s_sceneMonochrome.m` parity on the monochrome-image workflow, compare the CRT display wave and white SPD, the `cameraman.tif` monochrome scene geometry/luminance and source spectral summaries, and the D65-adjusted illuminant, spectral summaries, and rendered-RGB mean behavior.
- `scene_slanted_bar_small`: `s_sceneSlantedBar.m` parity on the slanted-edge scene workflow, compare the 256-pixel equal-photons slanted bar after luminance normalization, the no-ROI illuminant-energy plot output before and after the D65 illuminant swap, and the 128-pixel / 3.6-slope / 0.5-degree slanted bar geometry plus normalized center-row/center-column luminance profiles.
- `scene_harmonics_script_small`: `s_sceneHarmonics.m` parity on the harmonic-scene workflow, compare the four script parameter sets’ scene sizes, preserved mean luminance, and normalized center-row/center-column luminance profiles for the basic, phase-offset two-frequency, crossed-orientation, and symmetric crossed-frequency cases.
- `surface_munsell_small`: `s_surfaceMunsell.m` parity on the Munsell-surface data workflow, compare the raw XYZ/LAB shapes, illuminant-C wavelength/support vector, selected XYZ-to-sRGB chip samples and mean RGB, chromaticity/LAB summary bounds, and the first 45 hue/value/angle records printed by the script.
- `scene_demo_small`: `s_sceneDemo.m` parity on the introductory scene workflow, compare the `macbethd65` size/wave/luminance/photons invariants, the FOV mutation to `20 deg`, and the `freq orient pattern` bottom-row luminance plus `550 nm` radiance-line summaries.
- `scene_examples_small`: `s_sceneExamples.m` parity on the built-in scene catalog workflow, compare the deterministic example scenes’ size/wave-count/luminance/FOV summaries plus canonical center-row/center-column luminance profiles, and keep the default random reflectance-chart branch on geometry-only fields.
- `scene_roi_small`: `s_sceneRoi.m` parity on the default-scene ROI workflow, compare the ROI rectangle, ROI sample count, photons/energy/illuminant/reflectance summaries, the returned ROI-mean spectra, and exact manual-versus-direct reflectance agreement.
- `scene_rotate_small`: `s_sceneRotate.m` parity on the default star-pattern rotation workflow, compare selected frame angles, rotated frame sizes, luminance summaries, and canonicalized center-row/center-column luminance profiles.
- `scene_wavelength_small`: `s_sceneWavelength.m` parity on the default-scene wavelength-resampling workflow, compare the 10 nm, 5 nm, and 2 nm narrowband supports, preserved scene size and mean luminance, and normalized mean/center spectra for each scene state.
- `scene_hc_compress_small`: `s_sceneHCCompress.m` parity on the StuffedAnimals hypercube-compression workflow, compare the 95% and 99% basis counts, the save/reload 5 nm scene geometry and mean luminance, and the normalized mean/center spectra after the compressed multispectral roundtrip.
- `scene_increase_size_small`: `s_sceneIncreaseSize.m` parity on the default-scene pixel-replication workflow, compare the `[2 3]`, `[1 2]`, and `[3 1]` enlarged scene sizes, preserved mean luminance and normalized mean spectra, exact replay to the previous photon cube under stride decimation, and preserved aspect ratio from source to final scene.
- `scene_render_small`: `s_sceneRender.m` parity on the multispectral scene-render workflow, compare the D75-adjusted StuffedAnimals render summary plus the Feng Office and StuffedAnimals HDR-companded render summaries, center RGB samples, tone-mapping delta magnitudes, and canonicalized center-row luminance profiles.
- `scene_rgb2radiance_displays_small`: `t_sceneRGB2Radiance.m` parity on the display-driven RGB-to-radiance tutorial, compare `OLED-Sony`, `LCD-Apple`, and `CRT-Dell` white points, primary chromaticities, scene sizes/mean luminance, normalized mean SPDs, illuminant spectra, and rendered RGB summary statistics for `macbeth.tif`.
- `scene_surface_models_small`: `t_sceneSurfaceModels.m` parity on the Macbeth surface-model tutorial, compare Macbeth reflectance geometry and summary statistics, sign-canonicalized first four SVD basis functions, the 1-D through 4-D reconstruction RMSE trend, normalized D65 illuminant SPD, and the rendered 4x6 chart RGB summaries for the 1-D, 2-D, 3-D, 4-D, and full-basis cases.
- `display_create_lcd_example`: load and resample a display calibration; compare `spd`, `gamma`, and wave grid.
- `oi_psf_default_small`: `oiCreate('psf')` plus `oiCompute(..., crop=true)` parity on the default synthetic shift-invariant PSF optics path, compare wave axis and irradiance cube.
- `oi_si_lorentzian_small`: `siSynthetic('lorentzian', oi, gamma)` plus `oiSet('optics', optics)` and `oiCompute(..., crop=true)` parity on the script-driven synthetic PSF workflow from `s_opticsSIExamples.m`, compare wave axis and scale-invariant image-shape agreement on the final irradiance cube.
- `oi_psf550_si_lorentzian_small`: `siSynthetic('lorentzian', oi, gamma)` plus `oiPlot(oi, 'psf550')` parity on the script-driven shift-invariant Lorentzian workflow from `s_opticsSIExamples.m`, compare the returned spatial support grids and PSF surface at 550 nm.
- `oi_si_pillbox_small`: `siSynthetic('pillbox', oi, airyDisk(...))` plus `oiSet('optics', optics)` and `oiCompute(..., crop=true)` parity on the script-driven pillbox PSF workflow from `s_opticsSIExamples.m`, compare wave axis and the 550 nm input PSF center row.
- `oi_si_gaussian_ratio_small`: `siSynthetic('gaussian', oi, waveSpread, xyRatio)` plus `oiSet('optics', optics)` and `oiCompute(..., crop=true)` parity on the anisotropic Gaussian workflow from `s_opticsSIExamples.m`, compare wave axis plus the 550 nm input PSF center row.
- `oi_psf550_si_gaussian_ratio_small`: `siSynthetic('gaussian', oi, waveSpread, xyRatio)` plus `oiPlot(oi, 'psf550')` parity on the anisotropic Gaussian workflow from `s_opticsSIExamples.m`, compare the returned spatial support grids and PSF surface at 550 nm.
- `oi_illuminance_lines_si_gaussian_ratio_small`: `siSynthetic('gaussian', oi, waveSpread, xyRatio)` plus `oiPlot(oi, ' illuminance vline', xyMiddle, 'nofigure')` / `oiPlot(oi, ' illuminance hline', xyMiddle, 'nofigure')` parity on the anisotropic Gaussian workflow from `s_opticsSIExamples.m`, compare the returned horizontal and vertical illuminance line profiles through the image center.
- `oi_custom_otf_flare_small`: `opticsPSF2OTF('flare1.png')` plus `oiSet('optics otfstruct', OTF)` and `oiCompute(..., crop=true)` parity on the MATLAB custom-OTF flare workflow, compare wave axis, flare OTF diagnostics, and scale-invariant image-shape agreement on the final irradiance cube.
- `oi_ideal_otf_small`: `oiGet('optics OTF')`, `oiSet('optics OTF', ones(...))`, and `oiCompute(..., crop=true)` parity on the MATLAB ideal-OTF workflow from `s_opticsSIIdeal.m`, compare wave axis and normalized-MAE image agreement after replacing the stored shift-invariant OTF with an all-ones OTF.
- `oi_wvf_defocus_small`: `wvfSet(..., 'zcoeffs', [2, 0.5], {'defocus', 'vertical_astigmatism'})` plus `oiCompute(wvf, scene, crop=true)` parity on a script-driven WVF defocus/astigmatism workflow, compare wave axis, irradiance cube, and recovered named Zernike coefficients.
- `oi_wvf_script_defocus_small`: explicit `wvfCompute` plus `wvf2oi` and `oiCompute(..., crop=true)` parity on the MATLAB defocus script flow, compare wave axis, irradiance cube, defocus Zernike coefficient, pupil diameter, and resulting f-number.
- `wvf_astigmatism_small`: `s_wvfAstigmatism.m` parity on the named-Zernike astigmatism sweep, compare the returned support axis and normalized PSF center row/column profiles for the 3x3 defocus and vertical-astigmatism grid at 550 nm.
- `wvf_diffraction_small`: `s_wvfDiffraction.m` parity on the diffraction-limited WVF workflow, compare the initial Airy-disk and `wvf2oi` f-number match, the pupil-size sweeps at 550 nm and 400 nm, the human-LCA wavelength sweep, and focal-length `um per degree` scaling.
- `wvf_thibos_model_small`: `s_wvfThibosModel.m` parity on deterministic `ieMvnrnd`-driven virtual eyes, compare the drawn coefficient matrix, the sample-mean subject PSF center rows across 450/550/650 nm, and the selected example-subject PSF center rows and peaks at 450 nm and 550 nm.
- `wvf_zernike_set_small`: `s_wvfZernikeSet.m` parity on the defocus-plus-vertical-astigmatism scene loop, compare the 550 nm PSF center rows, the uncropped 550 nm OI center rows, and the returned `defocus` / `vertical_astigmatism` coefficients for `A = [-1, 0, 1]`.
- `wvf_wavefronts_small`: `s_wvfWavefronts.m` parity on the 16-term OSA-index wavefront sweep, compare the returned support axis, normalized wavefront center rows and columns, and peak absolute wavefront amplitude for indices 1:16.
- `zernike_interpolation_small`: `s_zernikeInterpolation.m` parity on the Double-Gauss field-height interpolation workflow, compare the interpolated and ground-truth Zernike coefficient vectors, their validation residual, and the normalized center-row PSFs for Zernike-space interpolation, ground truth, and direct space interpolation.
- `wvf_plot_script_sequence_small`: `s_wvfPlot.m` parity on the script-level wavefront plotting sequence, compare the 550 nm 1D PSF payloads in `um` and `mm`, the normalized 550 nm `mm` line payload, and the recalculated 460 nm PSF-angle / pupil-phase payloads after changing the computed wavelength.
- `optics_defocus_small`: `s_opticsDefocus.m` parity on the focus/DoF WVF sequence, compare mean-photon conservation across defocus/astigmatism reset, recovered Zernike coefficients, pupil-diameter updates, and normalized 550 nm center-row irradiance slices for the key blur states.
- `optics_defocus_displacement_small`: `s_opticsDefocusDisplacement.m` parity on the thin-lens image-plane displacement workflow, compare the base-power sweep curves, the ratio sweep inputs, the resulting displacements, and the constant displacement-to-focal-length ratio.
- `optics_dof_small`: `s_opticsDoF.m` parity on the thin-lens depth-of-field workflow, compare the 100 mm F/2 formula result, the CoC crossing-derived DOF estimate, the sampled CoC curve inputs, and the object-distance by f-number DOF sweep surface.
- `optics_depth_defocus_small`: `s_opticsDepthDefocus.m` parity on the thin-lens defocus workflow, compare the default-lens object-distance sweep, focal-plane relative defocus, image-distance curve, the shifted-image-plane defocus sweep and in-focus object distance, and the pupil-scaled Hopkins `w20` surface.
- `optics_defocus_scene_small`: `s_opticsDefocusScene.m` parity on the multispectral defocus-image workflow, compare the scene-driven sample-frequency vectors, the derived 10 um and 40 um defocus values, and the normalized 550 nm center-row blur states plus peak irradiance summaries for the focused, 5 D, 10 um, and 40 um OI outputs.
- `optics_defocus_wvf_small`: `s_opticsDefocusWVF.m` parity on the WVF-driven defocus workflow, compare the explicit diffraction-limited and 1.5-diopter branches against the alternate `oiCreate('wvf')` update path through the returned f-numbers, defocus coefficients, normalized 550 nm PSF center rows, and normalized 550 nm OI center rows.
- `optics_flare_small`: `s_opticsFlare.m` parity on the dirty-aperture flare workflow, compare the seeded aperture summaries, normalized 550 nm PSF center rows, PSF blur-width summaries, point-scene OI blur-width summaries, HDR-scene OI geometry, and normalized HDR irradiance ratios across the initial, five-sided, and defocused branches.
- `optics_flare2_small`: `s_opticsFlare2.m` parity on the second dirty-aperture flare workflow, compare the stable script outputs: aperture sums, point-scene blur-width summaries, HDR-scene OI geometry, and normalized HDR irradiance ratios across the six-sided, five-sided, and defocused branches.
- `optics_rt_synthetic_small`: `s_opticsRTSynthetic.m` parity on the synthetic ray-trace optics workflow, compare the geometry and relative-illumination curves, normalized 550 nm center/edge PSF slices, and the resulting point-array OI summary outputs.
- `optics_rt_gridlines_small`: `s_opticsRTGridLines.m` parity on the staged-versus-automated ray-trace gridline workflow, compare the geometry-only output, precomputed-PSF metadata, and normalized 550 nm center-row image slices for the staged ray-trace, automated ray-trace, and matched diffraction-limited wide/small-FOV branches.
- `optics_rt_psf_small`: `s_opticsRTPSF.m` parity on the upstream ray-trace PSF workflow, compare the sampled-PSF cache metadata, normalized 550 nm center/edge PSF slices, and the rendered point-array ray-trace versus diffraction-limited image summaries.
- `optics_rt_psf_view_small`: `s_opticsRTPSFView.m` parity on the upstream ray-trace PSF-view workflow, compare the sampled-PSF view metadata, normalized 550 nm field-height and angle-sweep PSF rows, the stable blur-width summaries for those PSF stacks, and the canonicalized center/edge `rtPlot(..., 'psf', ...)` row profiles.
- `wvf_compute_psf_small`: explicit `wvfPupilFunction` plus `wvfComputePSF(..., 'compute pupil func', false)` parity on the standalone MATLAB wavefront pupil/PSF workflow, compare PSF normalization, central PSF row, and pupil amplitude/phase rows.
- `wvf_psf2zcoeff_error_small`: direct `psf2zcoeff(...)` parity on the upstream PSF-to-Zernike error helper from `s_opticsPSF2Zcoeffs.m`, compare the scalar RMS error together with the wavelength and pupil-geometry inputs that drive the computation.
- `wvf_aperture_polygon_clean_small`: direct `wvfAperture(..., 'n sides', 8, 'dot mean', 0, 'line mean', 0, 'image rotate', 0)` parity on the deterministic clean polygon-aperture path, compare the full aperture image, its central row, image sum, and returned `nsides` metadata.
- `wvf_compute_aperture_polygon_small`: direct `wvfCompute(wvf, 'aperture', aperture)` parity on the deterministic polygon-aperture compute workflow from the upstream flare scripts, compare PSF normalization, central PSF row, pupil-amplitude row, and returned `nsides` metadata.
- `wvf_spatial_sampling_small`: direct `wvfGet(...)` parity on the MATLAB spatial-sampling workflow from `s_wvfSpatial.m`, compare wave list, calc-wave count, PSF sample spacing, pupil sample spacing, pupil positions, PSF x-axis data, and central pupil amplitude/phase rows.
- `wvf_spatial_controls_small`: direct `wvfSet(...)` / `wvfCompute(...)` parity on the MATLAB spatial-control workflow from `s_wvfSpatial.m`, compare how `npixels`, `pupil plane size`, and `focal length` change PSF sample spacing and `um per degree`.
- `oi_lswavelength_diffraction_small`: `oiPlot(oi, 'ls wavelength')` parity on a diffraction-limited OI configured like the upstream diffraction PSF script, compare the returned spatial axis, wavelength vector, and line-spread-by-wavelength surface.
- `oi_lswavelength_wvf_small`: `oiPlot(oi, 'ls wavelength')` parity on a WVF-derived OI configured like the upstream wavefront scripts, compare the returned spatial axis, wavelength vector, and line-spread-by-wavelength surface.
- `oi_otfwavelength_diffraction_small`: `oiPlot(oi, 'otf wavelength')` parity on a diffraction-limited OI configured like the upstream diffraction PSF script, compare the returned frequency axis, wavelength vector, and OTF-by-wavelength surface.
- `oi_otfwavelength_wvf_small`: `oiPlot(oi, 'otf wavelength')` parity on a WVF-derived OI configured like the upstream wavefront scripts, compare the returned frequency axis, wavelength vector, and OTF-by-wavelength surface.
- `oi_irradiance_hline_diffraction_lineep_small`: `oiPlot(oi, 'irradiance hline', [80 80])` parity on the diffraction-limited line-image workflow from `s_opticsImageFormation.m`, compare the returned spatial axis, wavelength vector, and irradiance mesh data.
- `optics_diffraction_small`: `s_opticsDiffraction.m` parity on the default-to-`f/12` diffraction-limited point-array workflow, compare the scene/oi geometry, unit-aware focal-length and pupil-diameter checks, the returned `psf 550` surface, the `ls wavelength` payload, and stable 550 nm image-blur width summaries.
- `oi_psf550_diffraction_small`: `oiPlot(oi, 'psf', [], 550)` parity on a diffraction-limited OI configured like the upstream diffraction PSF script, compare the returned spatial support grids and PSF surface.
- `oi_psfxaxis_diffraction_small`: `oiPlot(oi, 'psf xaxis', [], 550, 'um')` parity on a diffraction-limited OI configured like the upstream diffraction/WVF spatial scripts, compare the returned spatial support axis and PSF cross-section.
- `oi_psfyaxis_diffraction_small`: `oiPlot(oi, 'psf yaxis', [], 550, 'um')` parity on a diffraction-limited OI configured like the upstream diffraction/WVF spatial scripts, compare the returned spatial support axis and PSF cross-section.
- `optics_coc_small`: `opticsCoC(optics, oDist, 'unit', 'mm', 'n samples', 50)` parity on the upstream `s_opticsCoC.m` thin-lens workflow, compare the returned object-distance support and circle-of-confusion diameter sweeps for 50 mm F/2 and F/8 optics at 0.5 m and 3 m focus.
- `oi_cos4th_small`: `s_opticsCos4th.m` parity on the wide-field cos4th vignetting workflow, compare the default and 4x-focal-length horizontal illuminance line profiles plus their mean illuminance summaries for the uniform D65 80-degree scene.
- `oi_gaussian_psf_point_array_small`: `s_opticsGaussianPSF.m` parity on the point-array anisotropic Gaussian workflow, compare the interpolated three-sample wavelength support, padded OI size, and normalized blurred center row and column profiles across 450 nm, 550 nm, and 650 nm.
- `oi_psf_plot_diffraction_small`: `s_opticsPSFPlot.m` parity on the manual diffraction-limited PSF reconstruction workflow, compare the 600 nm `x`/`y` support grids, full PSF surface at `nSamp=100`, and the overlaid Airy-disk radius for `f/#=12`.
- `oi_pad_crop_small`: `s_opticsPadCrop.m` parity on the padded/cropped OI workflow, compare the padded OI size, computed crop rectangle, cropped OI size/FOV, the deterministic scene-FOV sensor line profiles from padded versus cropped OIs, and the padded-FOV sensor line profile.
- `optics_microlens_small`: `s_opticsMicrolens.m` parity on the default microlens workflow, compare the getter/setter payload, 30-degree sensor FOV setup, and the radiance-generated source/pixel irradiance center rows, support axis, and etendue.
- `optics_psf_to_otf_flare_small`: direct `opticsPSF2OTF(...)` parity on the upstream `flare1.png` workflow, compare the returned frequency axes plus a 550 nm OTF center crop and central row from the generated custom OTF struct.
- `oi_diffraction_limited_default`: default scene through `oiCreate()/oiCompute()`, compare irradiance cube, padding behavior, and FOV-derived geometry.
- `oi_wvf_small_scene`: small deterministic wavefront case, compare PSF/OTF-derived irradiance numerically with relaxed FFT tolerance.
- `sensor_bayer_noiseless`: default Bayer sensor with `noise flag = 0`, compare `volts` and exposure-derived scaling.
- `sensor_monochrome_noise_stats`: monochrome sensor with fixed seed and `noise flag = 2`, compare mean, std, and percentile bands, not bitwise samples.
- `ip_default_pipeline`: bilinear demosaic plus default rendering, compare `input`, `sensorspace`, and final RGB image with PSNR and MAE thresholds.
- `camera_default_pipeline`: `cameraCreate('default')` plus `cameraCompute(scene)`, compare final `ip.data.result` and intermediate stage metadata.
- Tolerances: deterministic scalar/vector math `rtol=1e-5`, `atol=1e-8`; FFT/convolution optics `rtol=1e-4`, `atol=1e-6`; final RGB `MAE <= 1e-3` and `PSNR >= 60 dB`; noise cases validated statistically within 1% on mean/std and 2% on percentile bands.
- `sensor_mcc_small`: `s_sensorMCC.m` parity on the chart-based sensor color-correction workflow, compare the scaled TIFF voltage summary, the headless Macbeth-derived CCM, and normalized pre/post-correction RGB summaries from the fixed-matrix IP path.
- `sensor_rolling_shutter_small`: `s_sensorRollingShutter.m` parity on the deterministic rolling-shutter assembly workflow, compare the per-frame mean-voltage trace, center-pixel temporal trace, final row summaries, and normalized final RGB summaries.
- `sensor_imx490_uniform_small`: `s_sensorIMX490.m` parity on the deterministic uniform-field IMX490 HDR workflow, compare the four capture names, capture mean electrons/volts/DV, large-gain and small-area ratios, combined-volts summary, and selected pixel-source counts.
- `sensor_hdr_pixel_size_small`: `s_sensorHDR_PixelSize.m` parity on the Feng Office HDR pixel-size sweep, compare the three sensor sizes, mean/p95 volts, mean electrons, and grayscale output summaries for the 1/2/4 micron monochrome sensors.
- `sensor_log_ar0132at_small`: `s_sensorLogAR0132AT.m` parity on the stable log-response AR0132AT workflow, compare the 1-second dynamic range, log-voltage summary statistics, and normalized 15th/114th-row line profiles sampled across the exponential ramp.
- `sensor_aliasing_small`: `s_sensorAliasing.m` parity on the stable aliasing workflow, compare the fine-pixel, coarse-pixel, and blurred-lens sweep-frequency line profiles plus canonicalized sharp/blurred slanted-bar sensor captures and their summary statistics.
- `sensor_external_analysis_small`: `s_sensorExternalAnalysis.m` parity on the asset-driven DUT setup workflow, compare the configured wave grid, loaded color/IR filter and QE spectra, CFA pattern, pixel metadata, imported `dutData.mat` volts plane, and stable voltage summary statistics.
- `sensor_macbeth_daylight_estimate_small`: `s_sensorMacbethDaylightEstimate.m` parity on the deterministic daylight-basis Macbeth solve, compare the reflectance matrix, sensor spectral QE, daylight basis in photons, generated camera-response matrix, stacked design system, and recovered normalized daylight weights and illuminant.
- `sensor_spectral_radiometer_small`: `s_spectralRadiometer.m` parity on the radiometer-sensor workflow, compare the radiometer sample wavelengths, explicit `1 x N` filter pattern, filter spectra, deterministic noise-free electron line, theoretical shot-noise curve, and stable noisy/full-image electron summary statistics.

## Validation Harness
- Maintain parity case definitions in `tests/parity/cases.yaml`.
- Each case runs twice: Octave executes the pinned upstream MATLAB functions and writes canonical outputs to `tests/parity/baselines/<case>.mat`; Python computes the same case and compares normalized outputs.
- Normalize before compare: squeeze singleton dims, convert MATLAB column vectors to 1D arrays, harmonize dtypes, and label units explicitly in the exported baseline payload.
- Do not use `oct2py`; invoke Octave as a subprocess so the harness behaves the same locally and in future CI.
- Keep Octave as a validation-only dependency. The Python library itself must run without Octave once baselines exist.

## Assumptions And Defaults
- Upstream baseline remains fixed at `412b9f9bdb3262f2552b96f0e769b5ad6cdff821` until milestone one ships.
- Package name is `pyisetcam`; the new repo is greenfield and does not mirror the MATLAB tree one-for-one.
- The Python port preserves MATLAB semantics first, not a fully redesigned Pythonic OO model.
- `vcSESSION` compatibility is deferred; omitted-object calls are errors in milestone one.
- Octave is the reference executor even if some GUI or MEX-heavy MATLAB features are unsupported there; those features are already out of scope.
- Upstream assets stay external in `.cache/upstream`; do not bulk-copy them into the Python package during milestone one.
- Load standard `.mat` files with `scipy.io.loadmat` and use `h5py` only as a fallback for v7.3/HDF5 assets.
- If parity exposes a numerically unstable MATLAB path, reproduce the documented outputs of the curated cases first rather than widening scope to match every historical edge case immediately.

## Post-Milestone Expansion Order
- Add more scene patterns and `sceneFromFile`.
- Expand sensor presets to RGBW, RCCC, and selected vendor models actually used in scripts.
- Port metrics and validation utilities.
- Expand ray-trace optics support from the current upstream-backed geometry / angle-aware PSF slice to fuller MATLAB fidelity beyond the newly landed whole-optics/raw-struct/psfStruct export, staged wrapper and OI-helper surface, padding/output bookkeeping, raw-table setter semantics, whole-struct optics replacement semantics, and the core PSF metadata `oiGet/oiSet` surface.
- Expand the new optional `SessionContext` compatibility layer beyond the initial object-registry helpers and session-aware core entry points, and only then consider GUI replacement.
