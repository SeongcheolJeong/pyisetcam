# Task Perception Model Profiles

These JSON profiles describe optional downstream perception model backends.
They are configuration templates only. The corresponding model packages and
weights are not core pyisetcam dependencies and must be installed/provided by
the user when the profile is used.
